# 后端费用报销方案

## 1. 功能概述

费用报销功能允许律师为案件进行费用报销申请。律师可以选择一个案件，选择一个律师银行账户，然后进行相关费用的报销，包括费用名称、金额和资料（票据）。

## 2. 数据库设计

### 2.1 表结构说明

费用报销功能包含三张核心数据表：

#### 2.1.1 tb_expense_reimbursement（费用报销主表）

存储费用报销单的基本信息，包括报销单号、关联案件、申请人、银行账户、总金额、审批状态等。

**核心字段说明：**

| 字段名 | 数据类型 | 说明 |
|--------|----------|------|
| id | bigint | 主键ID |
| reimbursement_number | varchar(50) | 报销单号，唯一索引 |
| case_id | bigint | 案件ID，关联tb_bankrupt_case表 |
| case_name | varchar(255) | 案件名称（冗余字段，便于查询） |
| applicant_id | bigint | 申请人ID，关联tb_user表 |
| applicant_name | varchar(100) | 申请人姓名（冗余字段，便于查询） |
| fund_account_id | bigint | 律师银行账户ID，关联tb_fund_account表 |
| fund_account_name | varchar(100) | 账户名称（冗余字段，便于查询） |
| bank_name | varchar(100) | 银行名称（冗余字段，便于查询） |
| bank_account | varchar(50) | 银行账号（冗余字段，便于查询） |
| total_amount | decimal(18,2) | 报销总金额（元） |
| reimbursement_date | date | 报销日期 |
| description | varchar(500) | 报销说明 |
| approval_status | varchar(20) | 审批状态：PENDING-待审批, APPROVED-已通过, REJECTED-已拒绝 |
| approver_id | bigint | 审批人ID |
| approver_name | varchar(100) | 审批人姓名 |
| approval_time | datetime | 审批时间 |
| approval_opinion | varchar(500) | 审批意见 |

**索引设计：**
- 唯一索引：uk_reimbursement_number（报销单号）
- 普通索引：idx_case_id、idx_applicant_id、idx_fund_account_id、idx_approval_status、idx_reimbursement_date、idx_create_time

**外键约束：**
- fk_expense_reimbursement_case：关联tb_bankrupt_case表，级联删除
- fk_expense_reimbursement_applicant：关联tb_user表，级联删除
- fk_expense_reimbursement_fund_account：关联tb_fund_account表，级联删除

#### 2.1.2 tb_expense_reimbursement_item（费用报销明细表）

存储每笔报销单的明细费用项，一个报销单可以包含多个费用明细。

**核心字段说明：**

| 字段名 | 数据类型 | 说明 |
|--------|----------|------|
| id | bigint | 主键ID |
| reimbursement_id | bigint | 报销单ID，关联tb_expense_reimbursement表 |
| item_name | varchar(100) | 费用名称 |
| item_amount | decimal(18,2) | 费用金额（元） |
| item_description | varchar(500) | 费用说明 |
| sort_order | int | 排序序号 |

**索引设计：**
- 普通索引：idx_reimbursement_id

**外键约束：**
- fk_expense_reimbursement_item_reimbursement：关联tb_expense_reimbursement表，级联删除

#### 2.1.3 tb_expense_reimbursement_attachment（费用报销附件表）

存储每笔报销单的附件信息（票据等），一个报销单可以包含多个附件。

**核心字段说明：**

| 字段名 | 数据类型 | 说明 |
|--------|----------|------|
| id | bigint | 主键ID |
| reimbursement_id | bigint | 报销单ID，关联tb_expense_reimbursement表 |
| file_name | varchar(255) | 文件名 |
| file_path | varchar(1000) | 文件路径 |
| file_size | bigint | 文件大小（字节） |
| file_type | varchar(50) | 文件类型 |
| upload_time | datetime | 上传时间 |
| sort_order | int | 排序序号 |

**索引设计：**
- 普通索引：idx_reimbursement_id

**外键约束：**
- fk_expense_reimbursement_attachment_reimbursement：关联tb_expense_reimbursement表，级联删除

### 2.2 数据关系图

```
tb_bankrupt_case (案件表)
    ↓ (1:N)
tb_expense_reimbursement (费用报销主表)
    ↓ (1:N)
    ├─→ tb_expense_reimbursement_item (费用报销明细表)
    └─→ tb_expense_reimbursement_attachment (费用报销附件表)

tb_user (用户表)
    ↓ (1:N)
tb_expense_reimbursement (申请人)

tb_fund_account (资金账户表)
    ↓ (1:N)
tb_expense_reimbursement (律师银行账户)
```

## 3. 后端接口设计

### 3.1 接口列表

| 接口路径 | 请求方法 | 功能说明 |
|----------|----------|----------|
| /api/expense-reimbursement | POST | 创建报销单 |
| /api/expense-reimbursement/{id} | GET | 查询报销单详情 |
| /api/expense-reimbursement | GET | 查询报销单列表 |
| /api/expense-reimbursement/{id} | PUT | 更新报销单 |
| /api/expense-reimbursement/{id} | DELETE | 删除报销单 |
| /api/expense-reimbursement/{id}/approve | POST | 审批报销单 |
| /api/expense-reimbursement/{id}/items | POST | 添加报销明细 |
| /api/expense-reimbursement/{id}/items/{itemId} | DELETE | 删除报销明细 |
| /api/expense-reimbursement/{id}/attachments | POST | 上传报销附件 |
| /api/expense-reimbursement/{id}/attachments/{attachmentId} | DELETE | 删除报销附件 |

### 3.2 接口详细说明

#### 3.2.1 创建报销单

**请求：**
```
POST /api/expense-reimbursement
Content-Type: application/json

{
  "caseId": 1,
  "fundAccountId": 1,
  "reimbursementDate": "2026-01-24",
  "description": "差旅费报销",
  "items": [
    {
      "itemName": "交通费",
      "itemAmount": 500.00,
      "itemDescription": "往返高铁票"
    },
    {
      "itemName": "住宿费",
      "itemAmount": 800.00,
      "itemDescription": "酒店住宿2晚"
    }
  ],
  "attachmentIds": [1, 2, 3]
}
```

**响应：**
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "reimbursementNumber": "BX202601240001",
    "caseId": 1,
    "caseName": "某某破产清算案",
    "applicantId": 10,
    "applicantName": "张律师",
    "fundAccountId": 1,
    "fundAccountName": "张律师账户",
    "bankName": "中国工商银行",
    "bankAccount": "6222021234567890",
    "totalAmount": 1300.00,
    "reimbursementDate": "2026-01-24",
    "description": "差旅费报销",
    "approvalStatus": "PENDING",
    "createTime": "2026-01-24 10:00:00"
  }
}
```

#### 3.2.2 查询报销单详情

**请求：**
```
GET /api/expense-reimbursement/1
```

**响应：**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "id": 1,
    "reimbursementNumber": "BX202601240001",
    "caseId": 1,
    "caseName": "某某破产清算案",
    "applicantId": 10,
    "applicantName": "张律师",
    "fundAccountId": 1,
    "fundAccountName": "张律师账户",
    "bankName": "中国工商银行",
    "bankAccount": "6222021234567890",
    "totalAmount": 1300.00,
    "reimbursementDate": "2026-01-24",
    "description": "差旅费报销",
    "approvalStatus": "PENDING",
    "items": [
      {
        "id": 1,
        "itemName": "交通费",
        "itemAmount": 500.00,
        "itemDescription": "往返高铁票"
      },
      {
        "id": 2,
        "itemName": "住宿费",
        "itemAmount": 800.00,
        "itemDescription": "酒店住宿2晚"
      }
    ],
    "attachments": [
      {
        "id": 1,
        "fileName": "高铁票.jpg",
        "filePath": "/uploads/expense/2026/01/24/xxx.jpg",
        "fileSize": 102400,
        "fileType": "image/jpeg",
        "uploadTime": "2026-01-24 10:00:00"
      }
    ],
    "createTime": "2026-01-24 10:00:00"
  }
}
```

#### 3.2.3 查询报销单列表

**请求：**
```
GET /api/expense-reimbursement?page=1&size=10&caseId=1&approvalStatus=PENDING
```

**响应：**
```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "total": 100,
    "page": 1,
    "size": 10,
    "records": [
      {
        "id": 1,
        "reimbursementNumber": "BX202601240001",
        "caseId": 1,
        "caseName": "某某破产清算案",
        "applicantId": 10,
        "applicantName": "张律师",
        "totalAmount": 1300.00,
        "reimbursementDate": "2026-01-24",
        "approvalStatus": "PENDING",
        "createTime": "2026-01-24 10:00:00"
      }
    ]
  }
}
```

#### 3.2.4 审批报销单

**请求：**
```
POST /api/expense-reimbursement/1/approve
Content-Type: application/json

{
  "approvalStatus": "APPROVED",
  "approvalOpinion": "同意报销"
}
```

**响应：**
```json
{
  "code": 200,
  "message": "审批成功",
  "data": {
    "id": 1,
    "approvalStatus": "APPROVED",
    "approverId": 5,
    "approverName": "李主任",
    "approvalTime": "2026-01-24 15:00:00",
    "approvalOpinion": "同意报销"
  }
}
```

## 4. 业务流程

### 4.1 创建报销单流程

1. 律师登录系统，进入费用报销页面
2. 选择案件（从案件列表中选择）
3. 选择律师银行账户（从资金账户列表中选择）
4. 填写报销日期和报销说明
5. 添加报销明细（费用名称、金额、说明）
6. 上传票据附件
7. 提交报销单
8. 系统自动生成报销单号（格式：BX + YYYYMMDD + 4位序号）
9. 系统自动计算总金额
10. 报销单状态默认为"待审批"

### 4.2 审批流程

1. 审批人登录系统，进入待审批报销单列表
2. 查看报销单详情，包括明细和附件
3. 填写审批意见
4. 选择审批结果（通过/拒绝）
5. 提交审批
6. 系统更新报销单状态和审批信息
7. 系统发送通知给申请人

### 4.3 报销单号生成规则

格式：BX + YYYYMMDD + 4位序号

示例：BX202601240001

生成逻辑：
1. 获取当前日期：YYYYMMDD
2. 查询当天已创建的报销单数量
3. 生成4位序号（不足4位前面补0）
4. 拼接生成报销单号

## 5. 权限控制

### 5.1 功能权限

| 权限代码 | 权限名称 | 说明 |
|----------|----------|------|
| expense:reimbursement:create | 创建报销单 | 允许创建费用报销单 |
| expense:reimbursement:view | 查看报销单 | 允许查看费用报销单 |
| expense:reimbursement:edit | 编辑报销单 | 允许编辑费用报销单 |
| expense:reimbursement:delete | 删除报销单 | 允许删除费用报销单 |
| expense:reimbursement:approve | 审批报销单 | 允许审批费用报销单 |

### 5.2 数据权限

- 律师只能查看和编辑自己创建的报销单
- 审批人可以查看所有待审批的报销单
- 管理员可以查看所有报销单

## 6. 技术实现要点

### 6.1 报销单号生成

使用分布式ID生成策略，确保报销单号的唯一性和连续性。可以考虑使用数据库序列或Redis实现。

### 6.2 附件上传

- 支持多种文件格式：图片（jpg、png、gif）、PDF、Word、Excel
- 文件大小限制：单个文件不超过10MB
- 文件存储路径：/uploads/expense/YYYY/MM/DD/
- 文件命名规则：UUID + 原文件扩展名

### 6.3 金额计算

- 总金额 = 所有明细金额之和
- 金额精度：保留2位小数
- 使用BigDecimal进行金额计算，避免精度丢失

### 6.4 事务处理

- 创建报销单时，需要同时创建明细和附件，使用事务保证数据一致性
- 删除报销单时，级联删除明细和附件
- 审批报销单时，更新报销单状态和审批信息

### 6.5 审计日志

- 记录所有报销单的创建、修改、删除、审批操作
- 记录操作人、操作时间、操作内容
- 便于后续审计和追溯

## 7. 异常处理

### 7.1 业务异常

| 异常代码 | 异常信息 | 处理方式 |
|----------|----------|----------|
| EXPENSE_001 | 案件不存在 | 返回错误提示，要求重新选择案件 |
| EXPENSE_002 | 银行账户不存在 | 返回错误提示，要求重新选择银行账户 |
| EXPENSE_003 | 报销金额不能为负数 | 返回错误提示，要求重新输入金额 |
| EXPENSE_004 | 报销单不存在 | 返回错误提示，检查报销单ID |
| EXPENSE_005 | 报销单已审批，不能修改 | 返回错误提示，只能查看 |
| EXPENSE_006 | 附件上传失败 | 返回错误提示，检查文件格式和大小 |

### 7.2 系统异常

- 数据库连接异常
- 文件上传异常
- 网络异常

## 8. 性能优化

### 8.1 数据库优化

- 合理使用索引，提高查询效率
- 对于大表查询，使用分页查询
- 避免N+1查询，使用JOIN查询获取关联数据

### 8.2 缓存优化

- 缓存案件列表、银行账户列表等基础数据
- 缓存报销单详情，减少数据库查询

### 8.3 文件存储优化

- 使用CDN加速文件访问
- 对图片文件进行压缩处理

## 9. 测试要点

### 9.1 功能测试

- 测试报销单的创建、查询、编辑、删除功能
- 测试报销明细的添加、删除功能
- 测试附件的上传、下载、删除功能
- 测试审批流程的完整性

### 9.2 性能测试

- 测试大量报销单的查询性能
- 测试大文件上传的性能
- 测试并发创建报销单的性能

### 9.3 安全测试

- 测试权限控制是否生效
- 测试SQL注入、XSS等安全漏洞
- 测试文件上传的安全性

## 10. 部署说明

### 10.1 数据库部署

执行SQL脚本：`sql/expense_reimbursement.sql`

### 10.2 配置说明

需要在配置文件中配置以下参数：

```yaml
# 文件上传配置
file:
  upload:
    path: /uploads/expense
    max-size: 10MB
    allowed-types: jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx

# 报销单号配置
expense:
  reimbursement:
    number-prefix: BX
    number-sequence-start: 1
```

### 10.3 权限配置

需要在权限表中添加费用报销相关权限，并分配给相应的角色。

## 11. 后续扩展

### 11.1 功能扩展

- 支持报销单打印
- 支持报销单导出Excel
- 支持报销统计报表
- 支持报销预算控制

### 11.2 集成扩展

- 与财务系统集成，实现自动付款
- 与OA系统集成，实现审批流程对接
- 与消息系统集成，实现实时通知

## 12. 版本信息

| 版本 | 日期 | 描述 |
|------|------|------|
| 1.0 | 2026-01-24 | 初始版本，完成费用报销功能设计 |
