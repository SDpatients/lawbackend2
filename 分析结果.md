# case/list 接口查询逻辑分析

## 接口实现分析

### 1. 接口路径和参数
- **请求路径**: `/case/list`
- **请求方法**: `GET`
- **参数**:
  - `pageNum`: 页码 (默认1)
  - `pageSize`: 每页大小 (默认10)
  - `caseStatus`: 案件状态 (可选)
  - `caseProgress`: 案件进度 (可选)

### 2. 核心查询逻辑

#### 2.1 控制器层 (BankruptCaseController.java:61-87)
1. 获取当前用户ID
2. 获取用户权限列表
3. 根据权限执行不同查询逻辑:
   - 若有 `case:query:all` 权限: 查询所有案件
   - 若有 `case:query:own` 权限: 查询用户相关案件
   - 否则: 返回空列表

#### 2.2 服务层 (BankruptCaseServiceImpl.java:174-202)
```java
@Override
public List<BankruptCase> getCaseList(Integer pageNum, Integer pageSize, String caseStatus, String caseProgress) {
    Pageable pageable = PageRequest.of(pageNum - 1, pageSize, Sort.by(Sort.Direction.DESC, "createTime"));

    Page<BankruptCase> page;
    if (caseStatus != null && caseProgress != null) {
        page = bankruptCaseRepository.findByCaseStatusAndCaseProgress(caseStatus, caseProgress, pageable);
    } else if (caseStatus != null) {
        page = bankruptCaseRepository.findByCaseStatus(caseStatus, pageable);
    } else if (caseProgress != null) {
        page = bankruptCaseRepository.findByCaseProgress(caseProgress, pageable);
    } else {
        page = bankruptCaseRepository.findAll(pageable);
    }

    return page.getContent();
}
```

### 3. 案件状态定义
在 `BankruptCase` 实体类中:
```java
@Column(name = "case_status", length = 50)
private String caseStatus = "ONGOING";
```
- 默认值为 "ONGOING"

## 没有返回数据的可能原因

### 1. 权限问题
- 用户可能没有 `case:query:all` 或 `case:query:own` 权限
- 检查当前登录用户的权限配置

### 2. 数据问题
- 数据库中可能没有状态为 "ONGOING" 的案件
- 检查数据库中 `tb_bankrupt_case` 表的 `case_status` 字段值

### 3. 参数问题
- 可能参数传递有误，例如 `caseStatus` 值与数据库中存储的值不匹配
- 检查前端传递的参数格式是否正确

### 4. 数据库问题
- 数据库连接可能有问题
- 数据库查询可能有异常

## 排查步骤

1. **检查用户权限**:
   - 确认当前登录用户是否有 `case:query:all` 或 `case:query:own` 权限
   - 查看权限表中的配置

2. **检查数据库数据**:
   - 执行 SQL 查询: `SELECT * FROM tb_bankrupt_case WHERE case_status = 'ONGOING'`
   - 确认是否存在符合条件的记录

3. **检查参数传递**:
   - 确认前端传递的 `caseStatus` 参数值是否为 "ONGOING"
   - 检查接口调用时的参数格式

4. **检查日志**:
   - 查看应用日志，确认是否有异常信息
   - 检查 SQL 执行情况

5. **测试其他状态**:
   - 尝试其他案件状态值，例如空值或其他状态
   - 确认是否能返回数据

## 解决方案

### 1. 权限配置
- 确保用户有适当的权限 (`case:query:all` 或 `case:query:own`)

### 2. 数据初始化
- 确保数据库中有状态为 "ONGOING" 的案件数据
- 检查案件创建和状态更新的逻辑

### 3. 参数验证
- 确保前端传递的参数格式正确
- 检查后端参数处理逻辑

### 4. 日志增强
- 在关键位置添加日志，便于排查问题
- 记录查询参数和返回结果

## 代码优化建议

1. **增加参数验证**:
   - 对 `caseStatus` 和 `caseProgress` 参数进行合法性验证
   - 提供参数错误的明确提示

2. **增加日志记录**:
   - 在 `getCaseList` 方法中添加详细日志
   - 记录查询条件和返回结果

3. **提供默认值处理**:
   - 当没有权限时，提供明确的错误信息而不是空列表
   - 当没有数据时，返回空列表但附带提示信息

4. **优化查询逻辑**:
   - 考虑使用更高效的查询方式
   - 增加缓存机制，提高查询性能

## 总结

`case/list` 接口的查询逻辑是基于用户权限和查询参数的组合查询。没有返回数据的原因可能是权限问题、数据问题、参数问题或数据库问题。通过逐步排查，可以找到具体原因并采取相应的解决方案。