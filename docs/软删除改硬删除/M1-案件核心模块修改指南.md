# M1 - 案件核心模块修改指南

## 模块说明

这是整个系统最核心的模块，包含破产案件的主数据和相关联的案件任务、进度、公告等数据。

**开发优先级**：⭐⭐⭐⭐⭐ (最高)

---

## 涉及的实体类

| 实体类 | 基类 | 主要字段 | 说明 |
|--------|------|----------|------|
| `BankruptCase` | `BaseEntity` | `isDeleted`, `caseNumber`(unique) | 破产案件主表 |
| `CaseTask` | `BaseEntity` | `isDeleted`, `caseId` | 案件任务 |
| `CaseTaskSubmission` | `BaseEntity` | `isDeleted`, `caseId` | 任务提交 |
| `CaseProgress` | `BaseEntity` | `isDeleted`, `caseId` | 案件进度 |
| `CaseProcessStage` | `BaseEntity` | `isDeleted`, `caseId` | 案件流程阶段 |
| `CaseAnnouncement` | `BaseEntity` | `isDeleted`, `caseId` | 案件公告 |

---

## 需要修改的文件清单

### 1. 实体类（Entity）
- [ ] `entity/BankruptCase.java`
- [ ] `entity/CaseTask.java`
- [ ] `entity/CaseTaskSubmission.java`
- [ ] `entity/CaseProgress.java`
- [ ] `entity/CaseProcessStage.java`
- [ ] `entity/CaseAnnouncement.java`

### 2. Repository接口
- [ ] `repository/BankruptCaseRepository.java`
- [ ] `repository/CaseTaskRepository.java`
- [ ] `repository/CaseTaskSubmissionRepository.java`
- [ ] `repository/CaseProgressRepository.java`
- [ ] `repository/CaseProcessStageRepository.java`
- [ ] `repository/CaseAnnouncementRepository.java`
- [ ] `repository/AnnouncementViewRecordRepository.java`

### 3. Service实现类
- [ ] `service/impl/BankruptCaseServiceImpl.java` ⚠️ **核心文件**
- [ ] `service/impl/CaseTaskServiceImpl.java`
- [ ] `service/impl/CaseTaskSubmissionServiceImpl.java`
- [ ] `service/impl/CaseProgressServiceImpl.java`
- [ ] `service/impl/CaseAnnouncementServiceImpl.java`

### 4. Controller类
- [ ] `controller/BankruptCaseController.java`
- [ ] `controller/CaseTaskController.java`
- [ ] `controller/CaseTaskSubmissionController.java`
- [ ] `controller/CaseProgressController.java`
- [ ] `controller/CaseProcessStageController.java`
- [ ] `controller/CaseAnnouncementController.java`

---

## 详细修改步骤

### 步骤1：修改实体类

**文件**: `entity/BankruptCase.java`

**修改内容**:
```java
// 当前代码（保持不变，isDeleted字段来自BaseEntity）
// 不需要修改此文件，只需确认它继承BaseEntity
```

**注意**: 所有实体都继承 `BaseEntity`，所以 `isDeleted` 字段在基类中定义。本次修改不需要删除实体中的字段，只需在Repository和Service中不再使用它。

---

### 步骤2：修改 Repository - BankruptCaseRepository

**文件**: `repository/BankruptCaseRepository.java`

**需要修改的方法**:

#### 2.1 查询方法 - 删除 `isDeleted = false` 条件

**修改前**:
```java
@Query("SELECT c FROM BankruptCase c WHERE c.isDeleted = false")
Page<BankruptCase> findAllActive(Pageable pageable);

@Query("SELECT COUNT(c) FROM BankruptCase c WHERE c.isDeleted = false")
Long countAllActive();

@Query("SELECT c FROM BankruptCase c WHERE c.isDeleted = false AND c.caseNumber = :caseNumber")
Optional<BankruptCase> findByCaseNumber(@Param("caseNumber") String caseNumber);
```

**修改后**:
```java
// 直接使用JPA默认方法，或删除 isDeleted 条件
Page<BankruptCase> findAll(Pageable pageable);

// 使用JPA默认方法
long count();

// 删除 isDeleted 条件
@Query("SELECT c FROM BankruptCase c WHERE c.caseNumber = :caseNumber")
Optional<BankruptCase> findByCaseNumber(@Param("caseNumber") String caseNumber);
```

#### 2.2 删除特殊方法 `findByCaseNumberIncludeDeleted`

**修改前**:
```java
@Query("SELECT c FROM BankruptCase c WHERE c.caseNumber = :caseNumber")
Optional<BankruptCase> findByCaseNumberIncludeDeleted(@Param("caseNumber") String caseNumber);
```

**修改后**:
```java
// 删除此方法，因为现在所有查询都不考虑 isDeleted
// 使用 findByCaseNumber 替代
```

#### 2.3 批量修改所有查询方法

所有包含 `WHERE c.isDeleted = false` 的 `@Query` 注解都需要修改为：
- 删除 `c.isDeleted = false AND` 部分
- 如果只剩 `WHERE`，则删除整个 `WHERE` 子句

**示例**:
```java
// 修改前
@Query("SELECT c FROM BankruptCase c WHERE c.isDeleted = false AND c.caseStatus = :caseStatus")
Page<BankruptCase> findByCaseStatus(@Param("caseStatus") String caseStatus, Pageable pageable);

// 修改后
@Query("SELECT c FROM BankruptCase c WHERE c.caseStatus = :caseStatus")
Page<BankruptCase> findByCaseStatus(@Param("caseStatus") String caseStatus, Pageable pageable);

// 或者更简单的写法（如果方法名符合JPA规范）
Page<BankruptCase> findByCaseStatus(String caseStatus, Pageable pageable);
```

---

### 步骤3：修改 Repository - 其他Repository

**文件**: 
- `repository/CaseTaskRepository.java`
- `repository/CaseTaskSubmissionRepository.java`
- `repository/CaseProgressRepository.java`
- `repository/CaseProcessStageRepository.java`
- `repository/CaseAnnouncementRepository.java`

**修改原则**: 同上，删除所有 `isDeleted = false` 条件

**特别注意** `deleteByCaseId` 方法:

**修改前**:
```java
// 可能是软删除实现
@Modifying
@Query("UPDATE CaseTask c SET c.isDeleted = true WHERE c.caseId = :caseId")
void deleteByCaseId(@Param("caseId") Long caseId);
```

**修改后**:
```java
// 改为硬删除
@Modifying
@Query("DELETE FROM CaseTask c WHERE c.caseId = :caseId")
void deleteByCaseId(@Param("caseId") Long caseId);

// 或者使用JPA默认方法
void deleteByCaseId(Long caseId);
```

---

### 步骤4：修改 Service - BankruptCaseServiceImpl ⚠️ 核心

**文件**: `service/impl/BankruptCaseServiceImpl.java`

这是最关键的文件，有两个地方需要修改：

#### 4.1 修改 `createCase` 方法

**当前代码** (约第161-225行):
```java
@Override
@Transactional(rollbackFor = Exception.class)
public BankruptCase createCase(CaseCreateRequest request, Long userId) {
    Optional<BankruptCase> existingCase = bankruptCaseRepository.findByCaseNumberIncludeDeleted(request.getCaseNumber());
    
    if (existingCase.isPresent()) {
        BankruptCase existing = existingCase.get();
        if (!existing.getIsDeleted()) {
            throw new BusinessException("案号已存在");
        }
        
        log.info("发现已删除的同序号案件，将恢复该记录...");
        
        hardDeleteRelatedData(existing.getId());
        
        existing.setIsDeleted(false);
        existing.setCaseName(request.getCaseName());
        // ... 更多字段设置
        
        BankruptCase savedCase = bankruptCaseRepository.save(existing);
        return savedCase;
    }
    
    // 创建新案件...
}
```

**修改后**:
```java
@Override
@Transactional(rollbackFor = Exception.class)
public BankruptCase createCase(CaseCreateRequest request, Long userId) {
    // 直接检查是否存在（硬删除后不存在已删除的记录）
    Optional<BankruptCase> existingCase = bankruptCaseRepository.findByCaseNumber(request.getCaseNumber());
    
    if (existingCase.isPresent()) {
        throw new BusinessException("案号已存在");
    }
    
    // 创建新案件
    BankruptCase bankruptCase = new BankruptCase();
    BeanUtils.copyProperties(request, bankruptCase, "debtClaimDeadline");
    
    if (request.getDebtClaimDeadline() != null) {
        bankruptCase.setDebtClaimDeadline(request.getDebtClaimDeadline().atStartOfDay());
    }
    
    bankruptCase.setCreateUserId(userId);
    bankruptCase.setUpdateUserId(userId);
    bankruptCase.setIsSimplifiedTrial(request.getIsSimplifiedTrial() != null && request.getIsSimplifiedTrial() == 1);
    bankruptCase.setCaseStatus("ONGOING");

    BankruptCase savedCase = bankruptCaseRepository.save(bankruptCase);
    
    caseTaskService.createTasksForCase(savedCase.getId());
    
    createInitialWorkTeam(savedCase.getId(), request.getMainResponsiblePerson(), request.getUndertakingPersonnel(), userId);

    return savedCase;
}
```

#### 4.2 修改 `deleteCase` 方法

**当前代码** (约第853-897行):
```java
@Override
@Transactional(rollbackFor = Exception.class)
public void deleteCase(Long caseId) {
    log.info("开始软删除案件及其关联数据, caseId: {}", caseId);

    BankruptCase bankruptCase = getCaseById(caseId);

    // 删除所有关联数据（这些Repository的deleteByCaseId已经是硬删除）
    approvalHistoryRepository.deleteByCaseId(caseId);
    approvalRepository.deleteByCaseId(caseId);
    // ... 更多删除操作

    // 软删除主案件
    bankruptCase.setIsDeleted(true);
    bankruptCaseRepository.save(bankruptCase);

    log.info("案件及其关联数据软删除成功，caseId: {}", caseId);
}
```

**修改后**:
```java
@Override
@Transactional(rollbackFor = Exception.class)
public void deleteCase(Long caseId) {
    log.info("开始硬删除案件及其关联数据, caseId: {}", caseId);

    BankruptCase bankruptCase = getCaseById(caseId);

    // 删除所有关联数据
    approvalHistoryRepository.deleteByCaseId(caseId);
    approvalRepository.deleteByCaseId(caseId);
    caseProcessStageRepository.deleteByCaseId(caseId);
    documentDeliveryRepository.deleteByCaseId(caseId);
    archiveRecordRepository.deleteByCaseId(caseId);
    caseAnnouncementRepository.deleteByCaseId(caseId);
    announcementViewRecordRepository.deleteByCaseId(caseId);
    fundReimbursementRepository.deleteByCaseId(caseId);
    fundFlowRepository.deleteByCaseId(caseId);
    fundOperationLogRepository.deleteByCaseId(caseId);
    fundBudgetRepository.deleteByCaseId(caseId);
    escrowManagementRepository.deleteByCaseId(caseId);
    fundAccountRepository.deleteByCaseId(caseId);
    fundApprovalRepository.deleteByCaseId(caseId);
    distributionDetailRepository.deleteByCaseId(caseId);
    distributionExecutionRepository.deleteByCaseId(caseId);
    commonDebtRepository.deleteByCaseId(caseId);
    claimConfirmationRepository.deleteByCaseId(caseId);
    bankruptcyExpenseRepository.deleteByCaseId(caseId);
    administratorRepository.deleteByCaseId(caseId);
    creditorClaimRepository.deleteByCaseId(caseId);
    workTeamPermissionRepository.deleteByCaseId(caseId);
    workTeamMemberRepository.deleteByCaseId(caseId);
    workTeamRepository.deleteByCaseId(caseId);
    workPlanRepository.deleteByCaseId(caseId);
    debtorEnterpriseRepository.deleteByCaseId(caseId);
    caseProgressRepository.deleteByCaseId(caseId);
    creditorInfoRepository.deleteByCaseId(caseId);
    caseTaskRepository.deleteByCaseId(caseId);
    caseTaskSubmissionRepository.deleteByCaseId(caseId);
    workLogRepository.deleteByCaseId(caseId);
    bankAccountRepository.deleteByCaseId(caseId);
    claimRegistrationRepository.deleteByCaseId(caseId);
    claimReviewRepository.deleteByCaseId(caseId);

    // 硬删除主案件（关键修改）
    bankruptCaseRepository.delete(bankruptCase);

    log.info("案件及其关联数据硬删除成功，caseId: {}", caseId);
}
```

#### 4.3 删除 `hardDeleteRelatedData` 方法

这个方法是之前为了处理软删除恢复而创建的，现在不再需要。

**修改后**: 直接删除整个 `hardDeleteRelatedData` 方法（约第899-952行）

---

### 步骤5：修改其他 Service

**文件**:
- `service/impl/CaseTaskServiceImpl.java`
- `service/impl/CaseTaskSubmissionServiceImpl.java`
- `service/impl/CaseProgressServiceImpl.java`
- `service/impl/CaseAnnouncementServiceImpl.java`

**修改原则**:
1. 查找所有软删除逻辑：`setIsDeleted(true)` + `save()`
2. 改为硬删除：`repository.delete(entity)` 或 `repository.deleteById(id)`

**示例**:
```java
// 修改前
public void deleteCaseTask(Long id) {
    CaseTask task = caseTaskRepository.findById(id).orElseThrow();
    task.setIsDeleted(true);
    caseTaskRepository.save(task);
}

// 修改后
public void deleteCaseTask(Long id) {
    caseTaskRepository.deleteById(id);
}
```

---

### 步骤6：修改 Controller

Controller 层通常不需要修改，因为删除接口的签名不变。但需要检查是否有特殊逻辑：

**检查点**:
1. 是否有恢复已删除数据的接口？（需要删除）
2. 是否有查询已删除数据的接口？（需要删除或修改）

---

## 验证清单

修改完成后，请按以下清单验证：

- [ ] 应用可以正常启动，无编译错误
- [ ] 创建新案件成功
- [ ] 删除案件后，数据库中该记录物理删除（`is_deleted` 字段不再相关）
- [ ] 删除案件后，关联的任务、进度等数据也被物理删除
- [ ] 删除后使用相同案号创建新案件，可以成功
- [ ] 查询案件列表正常，不显示已删除数据（因为已物理删除）
- [ ] 案件统计功能正常

---

## 常见问题

### Q1: 是否需要修改数据库表结构？
**A**: 建议暂时保留 `is_deleted` 字段，只修改代码逻辑。待全部验证通过后再考虑删除字段。

### Q2: 如果其他模块还在使用软删除怎么办？
**A**: 各模块独立修改，M1模块的修改不会影响其他模块。但 `BankruptCaseServiceImpl` 中的关联删除会调用其他模块的 Repository，需要确保那些 Repository 的 `deleteByCaseId` 方法也改为硬删除。

### Q3: 如何测试删除功能？
**A**: 
1. 创建一个新案件
2. 记录案件ID
3. 删除该案件
4. 查询数据库确认记录已物理删除：`SELECT * FROM tb_bankrupt_case WHERE id = ?` 应该返回空
5. 使用相同案号创建新案件，应该成功

---

## 关联影响

修改此模块会影响以下模块的调用：
- M2 债权模块：案件删除时会级联删除债权数据
- M3 资产管理模块：案件删除时会级联删除银行账户数据
- M4 资金管理模块：案件删除时会级联删除资金相关数据
- M5 文档管理模块：案件删除时会级联删除文档数据
- M6 工作管理模块：案件删除时会级联删除工作团队数据

**协调建议**: 建议先完成 M1 的核心修改，其他模块可以并行进行，但需要确保各自的 `deleteByCaseId` 方法正确实现。
