# 新增参数校验文档

## 文档说明

本文档详细说明了系统中所有新增API接口的参数校验规则，包括端点信息、参数定义、验证规则、示例请求和错误响应格式，供前端开发人员参考实现。

---

## 目录

1. [用户认证模块](#1-用户认证模块)
2. [用户管理模块](#2-用户管理模块)
3. [案件管理模块](#3-案件管理模块)
4. [债权人管理模块](#4-债权人管理模块)
5. [债务人管理模块](#5-债务人管理模块)
6. [法院管理模块](#6-法院管理模块)
7. [案件公告模块](#7-案件公告模块)
8. [案件进度模块](#8-案件进度模块)
9. [银行账户管理模块](#9-银行账户管理模块)
10. [资金账户管理模块](#10-资金账户管理模块)
11. [资金流水管理模块](#11-资金流水管理模块)
12. [资金审批管理模块](#12-资金审批管理模块)
13. [资金操作日志模块](#13-资金操作日志模块)
14. [工作团队管理模块](#14-工作团队管理模块)
15. [工作计划管理模块](#15-工作计划管理模块)
16. [管理人信息管理模块](#16-管理人信息管理模块)
17. [公告查看记录模块](#17-公告查看记录模块)
18. [通用错误响应格式](#通用错误响应格式)

---

## 1. 用户认证模块

### 1.1 用户登录

**API端点**: `POST /auth/login`

**请求DTO**: UserLoginRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| username | String | 必填 | @NotBlank - 用户名不能为空 |
| password | String | 必填 | @NotBlank - 密码不能为空 |
| smsCode | String | 可选 | @Size(min=4, max=6) - 验证码长度必须在4-6之间 |

**示例请求数据**:
```json
{
  "username": "admin",
  "password": "password123",
  "smsCode": "123456"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "用户名不能为空, 密码不能为空",
  "data": null
}
```

---

### 1.2 用户注册

**API端点**: `POST /auth/register`

**请求DTO**: UserRegisterRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| username | String | 必填 | @NotBlank - 用户名不能为空<br>@Size(min=3, max=50) - 用户名长度必须在3-50之间 |
| password | String | 必填 | @NotBlank - 密码不能为空<br>@Size(min=6, max=20) - 密码长度必须在6-20之间 |
| realName | String | 必填 | @NotBlank - 真实姓名不能为空<br>@Size(max=50) - 真实姓名长度不能超过50 |
| mobile | String | 必填 | @NotBlank - 手机号不能为空<br>@Pattern(regexp="^1[3-9]\\d{9}$") - 手机号格式不正确（中国大陆手机号格式） |
| email | String | 可选 | @Pattern(regexp="^[A-Za-z0-9+_.-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$") - 邮箱格式不正确 |
| smsCode | String | 必填 | @NotBlank - 短信验证码不能为空<br>@Size(min=4, max=6) - 验证码长度不正确 |

**示例请求数据**:
```json
{
  "username": "zhangsan",
  "password": "password123",
  "realName": "张三",
  "mobile": "13800138000",
  "email": "zhangsan@example.com",
  "smsCode": "123456"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "用户名长度必须在3-50之间, 手机号格式不正确",
  "data": null
}
```

---

### 1.3 刷新令牌

**API端点**: `POST /auth/refresh-token`

**请求DTO**: RefreshTokenRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| refreshToken | String | 必填 | @NotBlank - 刷新令牌不能为空 |

**示例请求数据**:
```json
{
  "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "刷新令牌不能为空",
  "data": null
}
```

---

### 1.4 修改密码

**API端点**: `POST /auth/change-password`

**请求DTO**: ChangePasswordRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| oldPassword | String | 必填 | @NotBlank - 原密码不能为空 |
| newPassword | String | 必填 | @NotBlank - 新密码不能为空<br>@Size(min=6, max=20) - 新密码长度必须在6-20之间 |

**示例请求数据**:
```json
{
  "oldPassword": "oldpassword123",
  "newPassword": "newpassword123"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "新密码长度必须在6-20之间",
  "data": null
}
```

---

### 1.5 发送短信验证码

**API端点**: `POST /auth/send-sms-code`

**请求DTO**: SendSmsCodeRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| mobile | String | 必填 | @NotBlank - 手机号不能为空<br>@Pattern(regexp="^1[3-9]\\d{9}$") - 手机号格式不正确 |
| smsType | String | 必填 | @NotBlank - 短信类型不能为空<br>@Pattern(regexp="^[1-3]$") - 短信类型不正确（1-3之间的数字） |

**示例请求数据**:
```json
{
  "mobile": "13800138000",
  "smsType": "1"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "手机号格式不正确",
  "data": null
}
```

---

## 2. 用户管理模块

### 2.1 创建用户

**API端点**: `POST /api/users`

**请求DTO**: UserCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| username | String | 必填 | @NotBlank - 用户名不能为空<br>@Size(min=3, max=50) - 用户名长度必须在3-50之间 |
| password | String | 必填 | @NotBlank - 密码不能为空<br>@Size(min=8, max=20) - 密码长度必须在8-20之间<br>@Pattern(regexp="^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*?&])[A-Za-z\\d@$!%*?&]+$") - 密码必须包含大小写字母、数字和特殊字符 |
| realName | String | 必填 | @NotBlank - 真实姓名不能为空<br>@Size(max=50) - 真实姓名长度不能超过50 |
| mobile | String | 必填 | @NotBlank - 手机号不能为空<br>@Pattern(regexp="^1[3-9]\\d{9}$") - 手机号格式不正确 |
| email | String | 可选 | @Email - 邮箱格式不正确 |
| phone | String | 可选 | @Pattern(regexp="^\\d{3,4}-?\\d{7,8}$") - 电话号码格式不正确（如：010-12345678） |
| status | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "username": "lisi",
  "password": "Password123@",
  "realName": "李四",
  "mobile": "13900139000",
  "email": "lisi@example.com",
  "phone": "010-12345678",
  "status": "ACTIVE"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "密码必须包含大小写字母、数字和特殊字符",
  "data": null
}
```

---

### 2.2 更新用户

**API端点**: `PUT /api/users/{id}`

**请求DTO**: UserUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| username | String | 可选 | @Size(min=3, max=50) - 用户名长度必须在3-50之间 |
| password | String | 可选 | @Size(min=8, max=20) - 密码长度必须在8-20之间<br>@Pattern(regexp="^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*?&])[A-Za-z\\d@$!%*?&]+$") - 密码必须包含大小写字母、数字和特殊字符 |
| realName | String | 可选 | @Size(max=50) - 真实姓名长度不能超过50 |
| mobile | String | 可选 | @Pattern(regexp="^1[3-9]\\d{9}$") - 手机号格式不正确 |
| email | String | 可选 | @Email - 邮箱格式不正确 |
| phone | String | 可选 | @Pattern(regexp="^\\d{3,4}-?\\d{7,8}$") - 电话号码格式不正确 |
| status | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "username": "lisi_new",
  "realName": "李四新",
  "mobile": "13900139001",
  "email": "lisi_new@example.com",
  "status": "ACTIVE"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "手机号格式不正确",
  "data": null
}
```

---

### 2.3 部分更新用户

**API端点**: `PATCH /api/users/{id}`

**请求DTO**: UserPatchRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| username | String | 可选 | @Size(min=3, max=50) - 用户名长度必须在3-50之间 |
| password | String | 可选 | @Size(min=8, max=20) - 密码长度必须在8-20之间<br>@Pattern(regexp="^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*?&])[A-Za-z\\d@$!%*?&]+$") - 密码必须包含大小写字母、数字和特殊字符 |
| realName | String | 可选 | @Size(max=50) - 真实姓名长度不能超过50 |
| mobile | String | 可选 | @Pattern(regexp="^1[3-9]\\d{9}$") - 手机号格式不正确 |
| email | String | 可选 | @Email - 邮箱格式不正确 |
| phone | String | 可选 | @Pattern(regexp="^\\d{3,4}-?\\d{7,8}$") - 电话号码格式不正确 |
| status | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "realName": "李四更新",
  "email": "lisi_updated@example.com"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "邮箱格式不正确",
  "data": null
}
```

---

### 2.4 更新用户状态

**API端点**: `PUT /api/users/{id}/status`

**请求DTO**: UpdateUserStatusRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| status | String | 必填 | @NotBlank - 用户状态不能为空<br>@Pattern(regexp="^(ACTIVE\|INACTIVE\|LOCKED\|DELETED)$") - 用户状态不正确（必须是ACTIVE、INACTIVE、LOCKED或DELETED之一） |

**示例请求数据**:
```json
{
  "status": "ACTIVE"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "用户状态不正确",
  "data": null
}
```

---

### 2.5 查询用户列表

**API端点**: `GET /api/users`

**请求DTO**: UserQueryRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| page | Integer | 可选 | @Min(value=1) - 页码必须大于0（默认值=1） |
| size | Integer | 可选 | @Min(value=1) - 每页数量必须大于0<br>@Max(value=100) - 每页数量不能超过100（默认值=10） |
| sortField | String | 可选 | 无验证规则（默认值="createTime"） |
| sortOrder | String | 可选 | 无验证规则（默认值="DESC"） |
| keyword | String | 可选 | 无验证规则 |
| status | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "page": 1,
  "size": 20,
  "sortField": "createTime",
  "sortOrder": "DESC",
  "keyword": "张",
  "status": "ACTIVE"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "页码必须大于0, 每页数量不能超过100",
  "data": null
}
```

---

## 3. 案件管理模块

### 3.1 创建案件

**API端点**: `POST /case`

**请求DTO**: CaseCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseNumber | String | 必填 | @NotBlank - 案号不能为空 |
| caseName | String | 必填 | @NotBlank - 案件名称不能为空 |
| acceptanceDate | LocalDate | 必填 | @NotNull - 受理日期不能为空 |
| caseSource | String | 可选 | 无验证规则 |
| acceptanceCourt | String | 可选 | 无验证规则 |
| designatedInstitution | String | 可选 | 无验证规则 |
| mainResponsiblePerson | String | 可选 | 无验证规则 |
| isSimplifiedTrial | Integer | 可选 | 无验证规则（默认值=0） |
| caseReason | String | 可选 | 无验证规则 |
| caseProgress | String | 可选 | 无验证规则（默认值="FIRST"） |
| debtClaimDeadline | LocalDateTime | 可选 | 无验证规则 |
| filingDate | LocalDate | 可选 | 无验证规则 |
| remarks | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseNumber": "（2024）破字第001号",
  "caseName": "某某公司破产清算案",
  "acceptanceDate": "2024-01-01",
  "caseSource": "法院指定",
  "acceptanceCourt": "北京市第一中级人民法院",
  "designatedInstitution": "某某律师事务所",
  "mainResponsiblePerson": "张律师",
  "isSimplifiedTrial": 0,
  "caseReason": "资不抵债",
  "caseProgress": "FIRST",
  "debtClaimDeadline": "2024-06-30T23:59:59",
  "filingDate": "2024-01-01",
  "remarks": "备注信息"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案号不能为空, 案件名称不能为空",
  "data": null
}
```

---

### 3.2 更新案件

**API端点**: `PUT /case/{caseId}`

**请求DTO**: CaseUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseName | String | 可选 | 无验证规则 |
| caseReason | String | 可选 | 无验证规则 |
| remarks | String | 可选 | 无验证规则 |
| filingDate | LocalDate | 可选 | 无验证规则 |
| caseProgress | String | 可选 | 无验证规则 |
| mainResponsiblePerson | String | 可选 | 无验证规则 |
| designatedInstitution | String | 可选 | 无验证规则 |
| acceptanceCourt | String | 可选 | 无验证规则 |
| debtClaimDeadline | LocalDateTime | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseName": "某某公司破产清算案（更新）",
  "caseReason": "资不抵债（更新）",
  "remarks": "备注信息更新",
  "filingDate": "2024-01-02",
  "caseProgress": "SECOND",
  "mainResponsiblePerson": "李律师",
  "designatedInstitution": "某某律师事务所（更新）",
  "acceptanceCourt": "北京市第一中级人民法院",
  "debtClaimDeadline": "2024-07-31T23:59:59"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "参数校验失败",
  "data": null
}
```

---

### 3.3 更新案件状态

**API端点**: `PUT /case/{caseId}/status`

**请求DTO**: CaseStatusUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseStatus | String | 必填 | @NotBlank - 案件状态不能为空 |
| remark | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseStatus": "IN_PROGRESS",
  "remark": "案件状态更新说明"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件状态不能为空",
  "data": null
}
```

---

### 3.4 更新案件进度

**API端点**: `PUT /case/{caseId}/progress`

**请求DTO**: CaseProgressUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| stageName | String | 可选 | 无验证规则 |
| stageDescription | String | 可选 | 无验证规则 |
| startDate | LocalDate | 可选 | 无验证规则 |
| endDate | LocalDate | 可选 | 无验证规则 |
| expectedEndDate | LocalDate | 可选 | 无验证规则 |
| progressStatus | String | 可选 | 无验证规则 |
| completionPercentage | Integer | 可选 | @Min(value=0) - 完成百分比不能小于0<br>@Max(value=100) - 完成百分比不能大于100 |
| keyTasks | String | 可选 | 无验证规则 |
| completedTasks | String | 可选 | 无验证规则 |
| pendingTasks | String | 可选 | 无验证规则 |
| issues | String | 可选 | 无验证规则 |
| solutions | String | 可选 | 无验证规则 |
| attachments | String | 可选 | 无验证规则 |
| responsiblePerson | String | 可选 | 无验证规则 |
| responsiblePersonId | Long | 可选 | 无验证规则 |
| remarks | String | 可选 | 无验证规则 |
| isCompleted | Boolean | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "stageName": "债权申报阶段",
  "stageDescription": "债权申报阶段描述",
  "startDate": "2024-01-01",
  "endDate": "2024-03-31",
  "expectedEndDate": "2024-03-31",
  "progressStatus": "IN_PROGRESS",
  "completionPercentage": 50,
  "keyTasks": "债权登记、债权审核",
  "completedTasks": "债权登记",
  "pendingTasks": "债权审核",
  "issues": "部分债权人未及时申报",
  "solutions": "发送提醒通知",
  "attachments": "附件ID列表",
  "responsiblePerson": "张律师",
  "responsiblePersonId": 1,
  "remarks": "备注信息",
  "isCompleted": false
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "完成百分比不能小于0/大于100",
  "data": null
}
```

---

### 3.5 案件审核

**API端点**: `POST /case/{caseId}/review`

**请求DTO**: CaseReviewRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| reviewStatus | String | 必填 | @NotNull - 审核状态不能为空 |
| reviewOpinion | String | 必填 | @NotBlank - 审核意见不能为空 |

**示例请求数据**:
```json
{
  "reviewStatus": "APPROVED",
  "reviewOpinion": "审核通过，案件材料齐全"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "审核状态不能为空, 审核意见不能为空",
  "data": null
}
```

---

## 4. 债权人管理模块

### 4.1 创建债权人

**API端点**: `POST /creditor`

**请求DTO**: CreditorCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 可选 | 无验证规则 |
| creditorName | String | 必填 | @NotBlank - 债权人名称不能为空 |
| creditorType | String | 必填 | @NotBlank - 债权人类型不能为空 |
| contactPhone | String | 可选 | 无验证规则 |
| contactEmail | String | 可选 | 无验证规则 |
| address | String | 可选 | 无验证规则 |
| idNumber | String | 可选 | 无验证规则 |
| legalRepresentative | String | 可选 | 无验证规则 |
| registeredCapital | BigDecimal | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "creditorName": "某某银行",
  "creditorType": "金融机构",
  "contactPhone": "010-12345678",
  "contactEmail": "bank@example.com",
  "address": "北京市朝阳区某某街道",
  "idNumber": "91110000123456789X",
  "legalRepresentative": "王行长",
  "registeredCapital": 100000000.00
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "债权人名称不能为空, 债权人类型不能为空",
  "data": null
}
```

---

### 4.2 更新债权人

**API端点**: `PUT /creditor/{creditorId}`

**请求DTO**: CreditorUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| creditorId | Long | 必填 | @NotNull - 债权人ID不能为空 |
| creditorName | String | 可选 | 无验证规则 |
| creditorType | String | 可选 | 无验证规则 |
| contactPhone | String | 可选 | 无验证规则 |
| contactEmail | String | 可选 | 无验证规则 |
| address | String | 可选 | 无验证规则 |
| idNumber | String | 可选 | 无验证规则 |
| legalRepresentative | String | 可选 | 无验证规则 |
| registeredCapital | BigDecimal | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "creditorId": 1,
  "creditorName": "某某银行（更新）",
  "creditorType": "金融机构",
  "contactPhone": "010-87654321",
  "contactEmail": "bank_new@example.com",
  "address": "北京市朝阳区某某街道（更新）",
  "idNumber": "91110000123456789X",
  "legalRepresentative": "李行长",
  "registeredCapital": 200000000.00
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "债权人ID不能为空",
  "data": null
}
```

---

### 4.3 创建债权申报

**API端点**: `POST /creditor-claim`

**请求DTO**: CreditorClaimCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| caseName | String | 可选 | 无验证规则 |
| debtor | String | 可选 | 无验证规则 |
| bankAccount | String | 可选 | 无验证规则 |
| creditorName | String | 必填 | @NotBlank - 债权人名称不能为空 |
| creditorType | String | 必填 | @NotBlank - 债权人类型不能为空 |
| creditCode | String | 可选 | 无验证规则 |
| legalRepresentative | String | 可选 | 无验证规则 |
| serviceAddress | String | 可选 | 无验证规则 |
| agentName | String | 可选 | 无验证规则 |
| agentPhone | String | 可选 | 无验证规则 |
| agentIdCard | String | 可选 | 无验证规则 |
| agentAddress | String | 可选 | 无验证规则 |
| accountName | String | 可选 | 无验证规则 |
| creditorBankAccount | String | 可选 | 无验证规则 |
| bankName | String | 可选 | 无验证规则 |
| principal | BigDecimal | 可选 | 无验证规则 |
| interest | BigDecimal | 可选 | 无验证规则 |
| penalty | BigDecimal | 可选 | 无验证规则 |
| otherLosses | BigDecimal | 可选 | 无验证规则 |
| totalAmount | BigDecimal | 必填 | @NotNull - 总金额不能为空 |
| hasCourtJudgment | Integer | 可选 | 无验证规则（默认值=0） |
| hasExecution | Integer | 可选 | 无验证规则（默认值=0） |
| hasCollateral | Integer | 可选 | 无验证规则（默认值=0） |
| claimNature | String | 可选 | 无验证规则 |
| claimType | String | 必填 | @NotBlank - 债权类型不能为空 |
| claimFacts | String | 可选 | 无验证规则 |
| claimNatureManager | String | 可选 | 无验证规则 |
| claimIdentifier | String | 可选 | 无验证规则 |
| evidenceList | String | 可选 | 无验证规则 |
| evidenceMaterials | String | 可选 | 无验证规则 |
| evidenceAttachments | String | 可选 | 无验证规则 |
| remarks | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "caseName": "某某公司破产清算案",
  "debtor": "某某公司",
  "bankAccount": "1234567890123456",
  "creditorName": "某某银行",
  "creditorType": "金融机构",
  "creditCode": "91110000123456789X",
  "legalRepresentative": "王行长",
  "serviceAddress": "北京市朝阳区某某街道",
  "agentName": "张三",
  "agentPhone": "13800138000",
  "agentIdCard": "110101199001011234",
  "agentAddress": "北京市朝阳区某某小区",
  "accountName": "某某银行",
  "creditorBankAccount": "9876543210987654",
  "bankName": "中国工商银行",
  "principal": 1000000.00,
  "interest": 50000.00,
  "penalty": 10000.00,
  "otherLosses": 5000.00,
  "totalAmount": 1065000.00,
  "hasCourtJudgment": 1,
  "hasExecution": 1,
  "hasCollateral": 0,
  "claimNature": "有担保债权",
  "claimType": "借款债权",
  "claimFacts": "借款合同纠纷",
  "claimNatureManager": "管理人",
  "claimIdentifier": "CL2024010001",
  "evidenceList": "证据1,证据2",
  "evidenceMaterials": "证据材料",
  "evidenceAttachments": "附件ID列表",
  "remarks": "备注信息"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件ID不能为空, 债权人名称不能为空, 债权类型不能为空",
  "data": null
}
```

---

### 4.4 更新债权申报

**API端点**: `PUT /creditor-claim/{claimId}`

**请求DTO**: CreditorClaimUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| creditorName | String | 可选 | 无验证规则 |
| principal | BigDecimal | 可选 | 无验证规则 |
| interest | BigDecimal | 可选 | 无验证规则 |
| penalty | BigDecimal | 可选 | 无验证规则 |
| otherLosses | BigDecimal | 可选 | 无验证规则 |
| totalAmount | BigDecimal | 可选 | 无验证规则 |
| claimType | String | 可选 | 无验证规则 |
| claimFacts | String | 可选 | 无验证规则 |
| remarks | String | 可选 | 无验证规则 |
| claimNature | String | 可选 | 无验证规则 |
| hasCourtJudgment | Integer | 可选 | 无验证规则 |
| hasExecution | Integer | 可选 | 无验证规则 |
| hasCollateral | Integer | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "creditorName": "某某银行（更新）",
  "principal": 1500000.00,
  "interest": 75000.00,
  "penalty": 15000.00,
  "otherLosses": 7500.00,
  "totalAmount": 1597500.00,
  "claimType": "借款债权",
  "claimFacts": "借款合同纠纷（更新）",
  "remarks": "备注信息更新",
  "claimNature": "有担保债权",
  "hasCourtJudgment": 1,
  "hasExecution": 1,
  "hasCollateral": 1
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "参数校验失败",
  "data": null
}
```

---

### 4.5 债权申报审核

**API端点**: `POST /creditor-claim/{claimId}/review`

**请求DTO**: CreditorClaimReviewRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| registrationStatus | String | 必填 | @NotNull - 登记状态不能为空 |
| reviewOpinion | String | 必填 | @NotBlank - 审核意见不能为空 |
| claimNatureManager | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "registrationStatus": "APPROVED",
  "reviewOpinion": "债权申报审核通过，材料齐全",
  "claimNatureManager": "管理人"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "登记状态不能为空, 审核意见不能为空",
  "data": null
}
```

---

## 5. 债务人管理模块

### 5.1 创建债务人

**API端点**: `POST /debtor`

**请求DTO**: DebtorCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| enterpriseName | String | 必填 | @NotBlank - 企业名称不能为空 |
| unifiedSocialCreditCode | String | 可选 | 无验证规则 |
| legalRepresentative | String | 必填 | @NotBlank - 法定代表人不能为空 |
| registrationAuthority | String | 可选 | 无验证规则 |
| establishmentDate | LocalDate | 可选 | 无验证规则 |
| registeredCapital | BigDecimal | 可选 | 无验证规则 |
| businessScope | String | 可选 | 无验证规则 |
| enterpriseType | String | 可选 | 无验证规则 |
| industry | String | 可选 | 无验证规则 |
| registeredAddress | String | 可选 | 无验证规则 |
| contactPhone | String | 可选 | @Pattern(regexp="^1[3-9]\\d{9}$") - 联系电话格式不正确 |
| contactPerson | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "enterpriseName": "某某科技有限公司",
  "unifiedSocialCreditCode": "91110000123456789X",
  "legalRepresentative": "张三",
  "registrationAuthority": "北京市市场监督管理局",
  "establishmentDate": "2010-01-01",
  "registeredCapital": 50000000.00,
  "businessScope": "技术开发、技术服务",
  "enterpriseType": "有限责任公司",
  "industry": "信息技术",
  "registeredAddress": "北京市朝阳区某某街道",
  "contactPhone": "13800138000",
  "contactPerson": "李四"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件ID不能为空, 企业名称不能为空, 法定代表人不能为空",
  "data": null
}
```

---

### 5.2 更新债务人

**API端点**: `PUT /debtor/{debtorId}`

**请求DTO**: DebtorUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| enterpriseName | String | 可选 | 无验证规则 |
| legalRepresentative | String | 可选 | 无验证规则 |
| contactPhone | String | 可选 | @Pattern(regexp="^1[3-9]\\d{9}$") - 联系电话格式不正确 |
| contactPerson | String | 可选 | 无验证规则 |
| businessScope | String | 可选 | 无验证规则 |
| industry | String | 可选 | 无验证规则 |
| registeredAddress | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "enterpriseName": "某某科技有限公司（更新）",
  "legalRepresentative": "王五",
  "contactPhone": "13900139000",
  "contactPerson": "赵六",
  "businessScope": "技术开发、技术服务、技术咨询",
  "industry": "信息技术",
  "registeredAddress": "北京市朝阳区某某街道（更新）"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "联系电话格式不正确",
  "data": null
}
```

---

## 6. 法院管理模块

### 6.1 创建法院

**API端点**: `POST /court`

**请求DTO**: CourtCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| fullName | String | 必填 | @NotBlank - 法院全称不能为空 |
| shortName | String | 必填 | @NotBlank - 法院简称不能为空 |
| courtLevel | String | 必填 | @NotBlank - 法院级别不能为空 |
| address | String | 可选 | 无验证规则 |
| contactPhone | String | 可选 | @Pattern(regexp="^1[3-9]\\d{9}$") - 联系电话格式不正确 |
| responsibleUserId | Long | 可选 | 无验证规则 |
| undertakingJudge | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "fullName": "北京市第一中级人民法院",
  "shortName": "北京一中院",
  "courtLevel": "中级人民法院",
  "address": "北京市朝阳区某某街道",
  "contactPhone": "13800138000",
  "responsibleUserId": 1,
  "undertakingJudge": "张法官"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "法院全称不能为空, 法院简称不能为空, 法院级别不能为空",
  "data": null
}
```

---

### 6.2 更新法院

**API端点**: `PUT /court/{courtId}`

**请求DTO**: CourtUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| fullName | String | 可选 | 无验证规则 |
| shortName | String | 可选 | 无验证规则 |
| contactPhone | String | 可选 | @Pattern(regexp="^1[3-9]\\d{9}$") - 联系电话格式不正确 |
| undertakingJudge | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "fullName": "北京市第一中级人民法院（更新）",
  "shortName": "北京一中院",
  "contactPhone": "13900139000",
  "undertakingJudge": "李法官"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "联系电话格式不正确",
  "data": null
}
```

---

## 7. 案件公告模块

### 7.1 创建案件公告

**API端点**: `POST /case-announcement`

**请求DTO**: CaseAnnouncementCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 必填 | @NotBlank - 案件ID不能为空 |
| title | String | 必填 | @NotBlank - 标题不能为空 |
| content | String | 必填 | @NotBlank - 内容不能为空 |
| announcementType | String | 必填 | @NotBlank - 公告类型不能为空 |
| attachments | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "title": "关于某某公司破产清算案的公告",
  "content": "公告内容详情...",
  "announcementType": "债权申报公告",
  "attachments": "附件ID列表"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件ID不能为空, 标题不能为空, 内容不能为空, 公告类型不能为空",
  "data": null
}
```

---

### 7.2 更新案件公告

**API端点**: `PUT /case-announcement/{announcementId}`

**请求DTO**: CaseAnnouncementUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| title | String | 可选 | 无验证规则 |
| content | String | 可选 | 无验证规则 |
| announcementType | String | 可选 | 无验证规则 |
| attachments | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "title": "关于某某公司破产清算案的公告（更新）",
  "content": "公告内容详情（更新）...",
  "announcementType": "债权申报公告",
  "attachments": "附件ID列表（更新）"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "参数校验失败",
  "data": null
}
```

---

### 7.3 发布案件公告

**API端点**: `POST /case-announcement/{announcementId}/publish`

**请求DTO**: CaseAnnouncementPublishRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| topExpireTime | LocalDateTime | 必填 | @NotNull - 置顶过期时间不能为空 |

**示例请求数据**:
```json
{
  "topExpireTime": "2024-12-31T23:59:59"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "置顶过期时间不能为空",
  "data": null
}
```

---

### 7.4 置顶案件公告

**API端点**: `POST /case-announcement/{announcementId}/top`

**请求DTO**: CaseAnnouncementPublishRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| topExpireTime | LocalDateTime | 必填 | @NotNull - 置顶过期时间不能为空 |

**示例请求数据**:
```json
{
  "topExpireTime": "2024-12-31T23:59:59"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "置顶过期时间不能为空",
  "data": null
}
```

---

## 8. 案件进度模块

### 8.1 创建案件进度

**API端点**: `POST /api/v1/case-progress`

**请求DTO**: CaseProgressCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| caseName | String | 可选 | 无验证规则 |
| caseNumber | String | 可选 | 无验证规则 |
| progressStage | String | 必填 | @NotBlank - 进度阶段不能为空 |
| stageName | String | 必填 | @NotBlank - 阶段名称不能为空 |
| stageDescription | String | 可选 | 无验证规则 |
| startDate | LocalDate | 可选 | 无验证规则 |
| expectedEndDate | LocalDate | 可选 | 无验证规则 |
| progressStatus | String | 可选 | 无验证规则 |
| completionPercentage | Integer | 可选 | @Min(value=0) - 完成百分比不能小于0<br>@Max(value=100) - 完成百分比不能大于100 |
| keyTasks | String | 可选 | 无验证规则 |
| completedTasks | String | 可选 | 无验证规则 |
| pendingTasks | String | 可选 | 无验证规则 |
| issues | String | 可选 | 无验证规则 |
| solutions | String | 可选 | 无验证规则 |
| attachments | String | 可选 | 无验证规则 |
| responsiblePerson | String | 可选 | 无验证规则 |
| responsiblePersonId | Long | 可选 | 无验证规则 |
| remarks | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "caseName": "某某公司破产清算案",
  "caseNumber": "（2024）破字第001号",
  "progressStage": "债权申报阶段",
  "stageName": "债权登记",
  "stageDescription": "债权登记阶段描述",
  "startDate": "2024-01-01",
  "expectedEndDate": "2024-03-31",
  "progressStatus": "IN_PROGRESS",
  "completionPercentage": 50,
  "keyTasks": "债权登记、债权审核",
  "completedTasks": "债权登记",
  "pendingTasks": "债权审核",
  "issues": "部分债权人未及时申报",
  "solutions": "发送提醒通知",
  "attachments": "附件ID列表",
  "responsiblePerson": "张律师",
  "responsiblePersonId": 1,
  "remarks": "备注信息"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件ID不能为空, 进度阶段不能为空, 阶段名称不能为空",
  "data": null
}
```

---

### 8.2 更新案件进度

**API端点**: `PUT /api/v1/case-progress/{progressId}`

**请求DTO**: CaseProgressUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| stageName | String | 可选 | 无验证规则 |
| stageDescription | String | 可选 | 无验证规则 |
| startDate | LocalDate | 可选 | 无验证规则 |
| endDate | LocalDate | 可选 | 无验证规则 |
| expectedEndDate | LocalDate | 可选 | 无验证规则 |
| progressStatus | String | 可选 | 无验证规则 |
| completionPercentage | Integer | 可选 | @Min(value=0) - 完成百分比不能小于0<br>@Max(value=100) - 完成百分比不能大于100 |
| keyTasks | String | 可选 | 无验证规则 |
| completedTasks | String | 可选 | 无验证规则 |
| pendingTasks | String | 可选 | 无验证规则 |
| issues | String | 可选 | 无验证规则 |
| solutions | String | 可选 | 无验证规则 |
| attachments | String | 可选 | 无验证规则 |
| responsiblePerson | String | 可选 | 无验证规则 |
| responsiblePersonId | Long | 可选 | 无验证规则 |
| remarks | String | 可选 | 无验证规则 |
| isCompleted | Boolean | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "stageName": "债权登记（更新）",
  "stageDescription": "债权登记阶段描述（更新）",
  "startDate": "2024-01-02",
  "endDate": "2024-04-30",
  "expectedEndDate": "2024-04-30",
  "progressStatus": "COMPLETED",
  "completionPercentage": 100,
  "keyTasks": "债权登记、债权审核",
  "completedTasks": "债权登记、债权审核",
  "pendingTasks": "",
  "issues": "",
  "solutions": "",
  "attachments": "附件ID列表（更新）",
  "responsiblePerson": "张律师",
  "responsiblePersonId": 1,
  "remarks": "备注信息（更新）",
  "isCompleted": true
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "完成百分比不能小于0/大于100",
  "data": null
}
```

---

## 9. 银行账户管理模块

### 9.1 创建银行账户

**API端点**: `POST /bank-account`

**请求DTO**: BankAccountCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| accountName | String | 必填 | @NotBlank - 账户名称不能为空 |
| bankName | String | 必填 | @NotBlank - 银行名称不能为空 |
| accountNumber | String | 必填 | @NotBlank - 银行账号不能为空<br>@Pattern(regexp="^[0-9]{10,30}$") - 银行账号格式不正确（10-30位数字） |
| accountType | String | 必填 | @NotBlank - 账户类型不能为空 |
| currency | String | 可选 | @Pattern(regexp="^[A-Z]{3}$") - 币种格式不正确（3位大写字母，默认值="CNY"） |
| currentBalance | BigDecimal | 必填 | @NotNull - 当前余额不能为空<br>@Positive - 当前余额必须大于0 |
| openingDate | LocalDate | 可选 | 无验证规则 |
| password | String | 必填 | @NotBlank - 密码不能为空 |

**示例请求数据**:
```json
{
  "accountName": "某某公司破产清算账户",
  "bankName": "中国工商银行",
  "accountNumber": "12345678901234567890",
  "accountType": "基本存款账户",
  "currency": "CNY",
  "currentBalance": 1000000.00,
  "openingDate": "2024-01-01",
  "password": "password123"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "账户名称不能为空, 银行账号格式不正确",
  "data": null
}
```

---

### 9.2 更新银行账户

**API端点**: `PUT /bank-account/{accountId}`

**请求DTO**: BankAccountUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| accountName | String | 必填 | @NotBlank - 账户名称不能为空 |
| currentBalance | BigDecimal | 必填 | @NotNull - 当前余额不能为空<br>@Positive - 当前余额必须大于0 |

**示例请求数据**:
```json
{
  "accountName": "某某公司破产清算账户（更新）",
  "currentBalance": 2000000.00
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "账户名称不能为空, 当前余额必须大于0",
  "data": null
}
```

---

### 9.3 更新银行账户密码

**API端点**: `PUT /bank-account/{accountId}/password`

**请求DTO**: BankAccountPasswordRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| oldPassword | String | 必填 | @NotBlank - 原密码不能为空 |
| newPassword | String | 必填 | @NotBlank - 新密码不能为空 |

**示例请求数据**:
```json
{
  "oldPassword": "oldpassword123",
  "newPassword": "newpassword123"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "原密码不能为空, 新密码不能为空",
  "data": null
}
```

---

### 9.4 更新银行账户状态

**API端点**: `PUT /bank-account/{accountId}/status`

**请求DTO**: BankAccountStatusRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| status | String | 必填 | @NotBlank - 状态不能为空<br>@Pattern(regexp="^(ACTIVE\|INACTIVE\|DELETED)$") - 状态值不正确（必须是ACTIVE、INACTIVE或DELETED之一） |

**示例请求数据**:
```json
{
  "status": "ACTIVE"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "状态不能为空, 状态值不正确",
  "data": null
}
```

---

## 10. 资金账户管理模块

### 10.1 创建资金账户

**API端点**: `POST /fund-account`

**请求DTO**: FundAccountCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| caseName | String | 必填 | @NotBlank - 案件名称不能为空 |
| accountName | String | 必填 | @NotBlank - 账户名称不能为空 |
| accountType | String | 必填 | @NotBlank - 账户类型不能为空 |
| initialBalance | BigDecimal | 必填 | @NotNull - 初始余额不能为空<br>@Positive - 初始余额必须大于0 |
| bankName | String | 必填 | @NotBlank - 银行名称不能为空 |
| bankAccount | String | 必填 | @NotBlank - 银行账号不能为空 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "caseName": "某某公司破产清算案",
  "accountName": "破产清算资金账户",
  "accountType": "专用账户",
  "initialBalance": 1000000.00,
  "bankName": "中国工商银行",
  "bankAccount": "12345678901234567890"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件ID不能为空, 案件名称不能为空, 账户名称不能为空",
  "data": null
}
```

---

### 10.2 更新资金账户

**API端点**: `PUT /fund-account/{fundAccountId}`

**请求DTO**: FundAccountUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| caseName | String | 必填 | @NotBlank - 案件名称不能为空 |
| accountName | String | 必填 | @NotBlank - 账户名称不能为空 |
| accountType | String | 必填 | @NotBlank - 账户类型不能为空 |
| bankName | String | 必填 | @NotBlank - 银行名称不能为空 |
| bankAccount | String | 必填 | @NotBlank - 银行账号不能为空 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "caseName": "某某公司破产清算案（更新）",
  "accountName": "破产清算资金账户（更新）",
  "accountType": "专用账户",
  "bankName": "中国工商银行",
  "bankAccount": "12345678901234567890"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件ID不能为空, 案件名称不能为空",
  "data": null
}
```

---

### 10.3 更新资金账户余额

**API端点**: `PUT /fund-account/{fundAccountId}/balance`

**请求DTO**: FundAccountBalanceRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| currentBalance | BigDecimal | 必填 | @NotNull - 当前余额不能为空<br>@Positive - 当前余额必须大于0 |

**示例请求数据**:
```json
{
  "currentBalance": 2000000.00
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "当前余额不能为空, 当前余额必须大于0",
  "data": null
}
```

---

### 10.4 更新资金账户状态

**API端点**: `PUT /fund-account/{fundAccountId}/status`

**请求DTO**: FundAccountStatusRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| status | String | 必填 | @NotNull - 状态不能为空 |

**示例请求数据**:
```json
{
  "status": "ACTIVE"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "状态不能为空",
  "data": null
}
```

---

## 11. 资金流水管理模块

### 11.1 创建资金流水

**API端点**: `POST /fund-flow`

**请求DTO**: FundFlowCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| caseName | String | 必填 | @NotBlank - 案件名称不能为空 |
| fundAccountId | Long | 必填 | @NotNull - 资金账户ID不能为空 |
| flowType | String | 必填 | @NotBlank - 流水类型不能为空<br>@Pattern(regexp="^(INCOME\|EXPENSE)$") - 流水类型不正确（必须是INCOME或EXPENSE之一） |
| amount | BigDecimal | 必填 | @NotNull - 金额不能为空<br>@Positive - 金额必须大于0 |
| balanceBefore | BigDecimal | 必填 | @NotNull - 操作前余额不能为空 |
| balanceAfter | BigDecimal | 必填 | @NotNull - 操作后余额不能为空 |
| transactionDate | LocalDateTime | 必填 | @NotNull - 交易日期不能为空 |
| description | String | 必填 | @NotBlank - 交易描述不能为空 |
| relatedDocument | String | 可选 | 无验证规则 |
| remark | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "caseName": "某某公司破产清算案",
  "fundAccountId": 1,
  "flowType": "INCOME",
  "amount": 100000.00,
  "balanceBefore": 900000.00,
  "balanceAfter": 1000000.00,
  "transactionDate": "2024-01-15T10:30:00",
  "description": "债权回收款项",
  "relatedDocument": "相关文档ID",
  "remark": "备注信息"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件ID不能为空, 流水类型不正确, 金额必须大于0",
  "data": null
}
```

---

### 11.2 更新资金流水

**API端点**: `PUT /fund-flow/{flowId}`

**请求DTO**: FundFlowUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| caseName | String | 必填 | @NotBlank - 案件名称不能为空 |
| fundAccountId | Long | 必填 | @NotNull - 资金账户ID不能为空 |
| flowType | String | 必填 | @NotBlank - 流水类型不能为空 |
| amount | BigDecimal | 必填 | @NotNull - 金额不能为空 |
| balanceBefore | BigDecimal | 必填 | @NotNull - 变动前余额不能为空 |
| balanceAfter | BigDecimal | 必填 | @NotNull - 变动后余额不能为空 |
| transactionDate | LocalDateTime | 必填 | @NotNull - 交易日期不能为空 |
| description | String | 可选 | 无验证规则 |
| relatedDocument | String | 可选 | 无验证规则 |
| operatorId | Long | 必填 | @NotNull - 操作人ID不能为空 |
| operationTime | LocalDateTime | 必填 | @NotNull - 操作时间不能为空 |
| remark | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "caseName": "某某公司破产清算案",
  "fundAccountId": 1,
  "flowType": "INCOME",
  "amount": 150000.00,
  "balanceBefore": 900000.00,
  "balanceAfter": 1050000.00,
  "transactionDate": "2024-01-15T10:30:00",
  "description": "债权回收款项（更新）",
  "relatedDocument": "相关文档ID",
  "operatorId": 1,
  "operationTime": "2024-01-15T10:35:00",
  "remark": "备注信息（更新）"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件ID不能为空, 操作人ID不能为空",
  "data": null
}
```

---

### 11.3 更新资金流水状态

**API端点**: `PUT /fund-flow/{flowId}/status`

**请求DTO**: FundFlowStatusRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| status | String | 必填 | @NotNull - 状态不能为空 |

**示例请求数据**:
```json
{
  "status": "ACTIVE"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "状态不能为空",
  "data": null
}
```

---

## 12. 资金审批管理模块

### 12.1 创建资金审批

**API端点**: `POST /fund-approval`

**请求DTO**: FundApprovalCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| flowId | Long | 必填 | @NotNull - 资金流水ID不能为空 |
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| amount | BigDecimal | 必填 | @NotNull - 金额不能为空<br>@Positive - 金额必须大于0 |
| approvalContent | String | 必填 | @NotBlank - 审批内容不能为空 |

**示例请求数据**:
```json
{
  "flowId": 1,
  "caseId": 1,
  "amount": 100000.00,
  "approvalContent": "债权回收款项审批"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "资金流水ID不能为空, 案件ID不能为空, 审批内容不能为空",
  "data": null
}
```

---

### 12.2 更新资金审批

**API端点**: `PUT /fund-approval/{approvalId}`

**请求DTO**: FundApprovalUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| flowId | Long | 必填 | @NotNull - 流程ID不能为空 |
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| amount | BigDecimal | 必填 | @NotNull - 金额不能为空 |
| approvalContent | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "flowId": 1,
  "caseId": 1,
  "amount": 150000.00,
  "approvalContent": "债权回收款项审批（更新）"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "流程ID不能为空, 案件ID不能为空",
  "data": null
}
```

---

### 12.3 审批资金

**API端点**: `POST /fund-approval/{approvalId}/approve`

**请求DTO**: FundApprovalRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| approvalStatus | String | 必填 | @NotBlank - 审批状态不能为空<br>@Pattern(regexp="^(APPROVED\|REJECTED)$") - 审批状态不正确（必须是APPROVED或REJECTED之一） |
| approvalOpinion | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "approvalStatus": "APPROVED",
  "approvalOpinion": "审批通过"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "审批状态不能为空, 审批状态不正确",
  "data": null
}
```

---

### 12.4 更新资金审批状态

**API端点**: `PUT /fund-approval/{approvalId}/status`

**请求DTO**: FundApprovalStatusRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| status | String | 必填 | @NotNull - 状态不能为空 |

**示例请求数据**:
```json
{
  "status": "APPROVED"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "状态不能为空",
  "data": null
}
```

---

## 13. 资金操作日志模块

### 13.1 创建资金操作日志

**API端点**: `POST /fund-operation-log`

**请求DTO**: FundOperationLogCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 可选 | 无验证规则 |
| operationType | String | 必填 | @NotBlank - 操作类型不能为空 |
| operationContent | String | 必填 | @NotBlank - 操作内容不能为空 |
| operatorId | Long | 必填 | @NotNull - 操作人ID不能为空 |
| ipAddress | String | 可选 | 无验证规则 |
| browserInfo | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "operationType": "CREATE",
  "operationContent": "创建资金账户",
  "operatorId": 1,
  "ipAddress": "192.168.1.100",
  "browserInfo": "Chrome/120.0.0.0"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "操作类型不能为空, 操作内容不能为空, 操作人ID不能为空",
  "data": null
}
```

---

### 13.2 更新资金操作日志

**API端点**: `PUT /fund-operation-log/{logId}`

**请求DTO**: FundOperationLogUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 可选 | 无验证规则 |
| operationType | String | 必填 | @NotBlank - 操作类型不能为空 |
| operationContent | String | 必填 | @NotBlank - 操作内容不能为空 |
| operatorId | Long | 必填 | @NotBlank - 操作人ID不能为空 |
| ipAddress | String | 可选 | 无验证规则 |
| browserInfo | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "operationType": "UPDATE",
  "operationContent": "更新资金账户（更新）",
  "operatorId": 1,
  "ipAddress": "192.168.1.100",
  "browserInfo": "Chrome/120.0.0.0"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "操作类型不能为空, 操作内容不能为空",
  "data": null
}
```

---

### 13.3 更新资金操作日志状态

**API端点**: `PUT /fund-operation-log/{logId}/status`

**请求DTO**: FundOperationLogStatusRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| status | String | 必填 | @NotNull - 状态不能为空 |

**示例请求数据**:
```json
{
  "status": "ACTIVE"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "状态不能为空",
  "data": null
}
```

---

## 14. 工作团队管理模块

### 14.1 创建工作团队

**API端点**: `POST /work-team`

**请求DTO**: WorkTeamCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| teamName | String | 必填 | @NotBlank - 团队名称不能为空 |
| teamLeaderId | Long | 必填 | @NotNull - 团队负责人ID不能为空 |
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| teamDescription | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "teamName": "破产清算工作组",
  "teamLeaderId": 1,
  "caseId": 1,
  "teamDescription": "负责某某公司破产清算工作"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "团队名称不能为空, 团队负责人ID不能为空, 案件ID不能为空",
  "data": null
}
```

---

### 14.2 更新工作团队

**API端点**: `PUT /work-team/{teamId}`

**请求DTO**: WorkTeamUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| teamId | Long | 必填 | @NotNull - 团队ID不能为空 |
| teamName | String | 可选 | 无验证规则 |
| teamDescription | String | 可选 | 无验证规则 |
| status | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "teamId": 1,
  "teamName": "破产清算工作组（更新）",
  "teamDescription": "负责某某公司破产清算工作（更新）",
  "status": "ACTIVE"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "团队ID不能为空",
  "data": null
}
```

---

### 14.3 添加工作团队成员

**API端点**: `POST /work-team/{teamId}/member`

**请求DTO**: WorkTeamMemberCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |
| userId | Long | 必填 | @NotNull - 用户ID不能为空 |
| teamRole | String | 必填 | @NotBlank - 团队角色不能为空 |
| permissionLevel | String | 可选 | @Pattern(regexp="^(VIEW\|EDIT\|ADMIN)$") - 权限级别不正确（必须是VIEW、EDIT或ADMIN之一） |

**示例请求数据**:
```json
{
  "caseId": 1,
  "userId": 1,
  "teamRole": "律师",
  "permissionLevel": "EDIT"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件ID不能为空, 用户ID不能为空, 团队角色不能为空",
  "data": null
}
```

---

### 14.4 更新工作团队成员权限

**API端点**: `PUT /work-team/work-team-member/{memberId}/permission`

**请求DTO**: WorkTeamMemberPermissionRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| permissionLevel | String | 必填 | @NotBlank - 权限级别不能为空<br>@Pattern(regexp="^(VIEW\|EDIT\|ADMIN)$") - 权限级别不正确（必须是VIEW、EDIT或ADMIN之一） |
| permissionType | String | 必填 | @NotBlank - 权限类型不能为空 |

**示例请求数据**:
```json
{
  "permissionLevel": "ADMIN",
  "permissionType": "案件管理"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "权限级别不能为空, 权限级别不正确, 权限类型不能为空",
  "data": null
}
```

---

### 14.5 分配工作团队成员权限

**API端点**: `POST /work-team/work-team-member/{memberId}/permissions`

**请求DTO**: AssignWorkTeamPermissionsRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| permissions | List<WorkTeamPermissionItem> | 必填 | @NotEmpty - 权限列表不能为空<br>@Valid - 嵌套对象验证 |

**WorkTeamPermissionItem**:
| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| moduleType | String | 可选 | 无验证规则 |
| permissionType | String | 可选 | 无验证规则 |
| isAllowed | Integer | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "permissions": [
    {
      "moduleType": "案件管理",
      "permissionType": "查看",
      "isAllowed": 1
    },
    {
      "moduleType": "案件管理",
      "permissionType": "编辑",
      "isAllowed": 1
    }
  ]
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "权限列表不能为空",
  "data": null
}
```

---

## 15. 工作计划管理模块

### 15.1 创建工作计划

**API端点**: `POST /work-plan`

**请求DTO**: WorkPlanCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| planNumber | String | 必填 | @NotBlank - 计划编号不能为空 |
| planType | String | 必填 | @NotBlank - 计划类型不能为空 |
| planContent | String | 必填 | @NotBlank - 计划内容不能为空 |
| startDate | LocalDate | 必填 | @NotNull - 开始日期不能为空 |
| endDate | LocalDate | 必填 | @NotNull - 结束日期不能为空 |
| responsibleUserId | Long | 必填 | @NotNull - 负责人ID不能为空 |
| caseId | Long | 必填 | @NotNull - 案件ID不能为空 |

**示例请求数据**:
```json
{
  "planNumber": "WP2024010001",
  "planType": "债权申报计划",
  "planContent": "完成债权申报工作",
  "startDate": "2024-01-01",
  "endDate": "2024-03-31",
  "responsibleUserId": 1,
  "caseId": 1
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "计划编号不能为空, 计划类型不能为空, 计划内容不能为空",
  "data": null
}
```

---

### 15.2 更新工作计划

**API端点**: `PUT /work-plan/{planId}`

**请求DTO**: WorkPlanUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| planNumber | String | 必填 | @NotBlank - 计划编号不能为空 |
| planType | String | 必填 | @NotBlank - 计划类型不能为空 |
| planContent | String | 必填 | @NotBlank - 计划内容不能为空 |
| startDate | LocalDate | 可选 | 无验证规则 |
| endDate | LocalDate | 可选 | 无验证规则 |
| responsibleUserId | Long | 可选 | 无验证规则 |
| caseId | Long | 可选 | 无验证规则 |
| executionStatus | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "planNumber": "WP2024010001",
  "planType": "债权申报计划",
  "planContent": "完成债权申报工作（更新）",
  "startDate": "2024-01-02",
  "endDate": "2024-04-30",
  "responsibleUserId": 1,
  "caseId": 1,
  "executionStatus": "IN_PROGRESS"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "计划编号不能为空, 计划类型不能为空",
  "data": null
}
```

---

### 15.3 更新工作计划状态

**API端点**: `PUT /work-plan/{planId}/execution-status`

**请求DTO**: WorkPlanStatusRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| executionStatus | String | 必填 | @NotBlank - 执行状态不能为空<br>@Pattern(regexp="^(NOT_STARTED\|IN_PROGRESS\|COMPLETED\|DELAYED\|CANCELLED)$") - 执行状态不正确（必须是NOT_STARTED、IN_PROGRESS、COMPLETED、DELAYED或CANCELLED之一） |

**示例请求数据**:
```json
{
  "executionStatus": "IN_PROGRESS"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "执行状态不能为空, 执行状态不正确",
  "data": null
}
```

---

## 16. 管理人信息管理模块

### 16.1 创建管理人

**API端点**: `POST /administrator`

**请求DTO**: AdministratorCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| caseId | Long | 必填 | @NotBlank - 案件ID不能为空 |
| administratorType | String | 必填 | @NotBlank - 管理人类型不能为空 |
| responsiblePersonId | Long | 可选 | 无验证规则 |
| contactPhone | String | 可选 | 无验证规则 |
| contactEmail | String | 可选 | 无验证规则 |
| officeAddress | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "caseId": 1,
  "administratorType": "律师事务所",
  "responsiblePersonId": 1,
  "contactPhone": "010-12345678",
  "contactEmail": "admin@example.com",
  "officeAddress": "北京市朝阳区某某街道"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "案件ID不能为空, 管理人类型不能为空",
  "data": null
}
```

---

### 16.2 更新管理人

**API端点**: `PUT /administrator/{administratorId}`

**请求DTO**: AdministratorUpdateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| contactPhone | String | 可选 | 无验证规则 |
| contactEmail | String | 可选 | 无验证规则 |
| officeAddress | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "contactPhone": "010-87654321",
  "contactEmail": "admin_new@example.com",
  "officeAddress": "北京市朝阳区某某街道（更新）"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "参数校验失败",
  "data": null
}
```

---

### 16.3 创建管理人工作人员

**API端点**: `POST /administrator/{administratorId}/staff`

**请求DTO**: AdministratorStaffCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| administratorId | Long | 必填 | @NotBlank - 管理人ID不能为空 |
| name | String | 必填 | @NotBlank - 姓名不能为空 |
| staffType | String | 必填 | @NotBlank - 人员类型不能为空 |
| idNumber | String | 可选 | 无验证规则 |
| lawyerLicenseNumber | String | 可选 | 无验证规则 |
| contactPhone | String | 可选 | 无验证规则 |
| email | String | 可选 | 无验证规则 |
| responsibility | String | 可选 | 无验证规则 |
| appointmentDate | LocalDate | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "administratorId": 1,
  "name": "张三",
  "staffType": "律师",
  "idNumber": "110101199001011234",
  "lawyerLicenseNumber": "123456789012345",
  "contactPhone": "13800138000",
  "email": "zhangsan@example.com",
  "responsibility": "负责债权审核",
  "appointmentDate": "2024-01-01"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "管理人ID不能为空, 姓名不能为空, 人员类型不能为空",
  "data": null
}
```

---

## 17. 公告查看记录模块

### 17.1 创建公告查看记录

**API端点**: `POST /api/v1/announcement-view-record`

**请求DTO**: AnnouncementViewRecordCreateRequest

| 参数名 | 数据类型 | 必填/可选 | 验证规则 |
|--------|----------|-----------|----------|
| announcementId | Long | 必填 | @NotNull - 公告ID不能为空 |
| announcementTitle | String | 可选 | 无验证规则 |
| caseId | Long | 可选 | 无验证规则 |
| caseName | String | 可选 | 无验证规则 |
| viewerId | Long | 可选 | 无验证规则 |
| viewerName | String | 可选 | 无验证规则 |
| viewerType | String | 可选 | 无验证规则 |
| ipAddress | String | 可选 | 无验证规则 |
| userAgent | String | 可选 | 无验证规则 |
| viewDuration | Integer | 可选 | 无验证规则 |
| deviceType | String | 可选 | 无验证规则 |
| browserType | String | 可选 | 无验证规则 |
| osType | String | 可选 | 无验证规则 |
| location | String | 可选 | 无验证规则 |

**示例请求数据**:
```json
{
  "announcementId": 1,
  "announcementTitle": "关于某某公司破产清算案的公告",
  "caseId": 1,
  "caseName": "某某公司破产清算案",
  "viewerId": 1,
  "viewerName": "张三",
  "viewerType": "债权人",
  "ipAddress": "192.168.1.100",
  "userAgent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
  "viewDuration": 120,
  "deviceType": "PC",
  "browserType": "Chrome",
  "osType": "Windows",
  "location": "北京市"
}
```

**验证失败错误响应**:
```json
{
  "code": 400,
  "message": "公告ID不能为空",
  "data": null
}
```

---

## 通用错误响应格式

### 错误响应结构

所有API在验证失败时都会返回统一的错误响应格式：

```json
{
  "code": 400,
  "message": "错误信息描述",
  "data": null
}
```

### 常见错误代码

| 错误代码 | HTTP状态码 | 说明 |
|---------|-----------|------|
| 400 | 400 Bad Request | 参数校验失败、参数绑定失败、约束校验失败、JSON解析失败 |
| 429 | 429 Too Many Requests | 请求频率限制 |
| 500 | 500 Internal Server Error | 系统异常 |

### 常见验证错误类型

1. **参数校验失败** (MethodArgumentNotValidException)
   - 触发条件：@RequestBody参数验证失败
   - 错误代码：400
   - 错误信息：字段级别的验证错误信息，多个错误用逗号分隔

2. **参数绑定失败** (BindException)
   - 触发条件：表单参数绑定失败
   - 错误代码：400
   - 错误信息：字段级别的验证错误信息

3. **约束校验失败** (ConstraintViolationException)
   - 触发条件：路径参数或查询参数验证失败
   - 错误代码：400
   - 错误信息：约束级别的验证错误信息

4. **JSON解析失败** (HttpMessageNotReadableException)
   - 触发条件：请求体JSON格式错误
   - 错误代码：400
   - 错误信息："请求参数格式错误,请检查JSON格式"

5. **业务异常** (BusinessException)
   - 触发条件：业务逻辑校验失败
   - 错误代码：自定义业务错误代码
   - 错误信息：业务错误描述

6. **请求频率限制** (RateLimitException)
   - 触发条件：请求超过频率限制
   - 错误代码：自定义频率限制错误代码
   - 错误信息：频率限制错误描述

### 常用验证注解说明

| 注解 | 适用类型 | 说明 |
|------|---------|------|
| @NotNull | 对象类型 | 字段不能为null |
| @NotBlank | String | 字段不能为null、空或仅包含空格 |
| @NotEmpty | 集合/数组/Map | 集合不能为null或空 |
| @Size(min=, max=) | String/集合 | 字符串或集合长度在指定范围内 |
| @Min(value=) | 数值类型 | 数值必须大于等于指定值 |
| @Max(value=) | 数值类型 | 数值必须小于等于指定值 |
| @Positive | 数值类型 | 数值必须大于0 |
| @Email | String | 必须是有效的邮箱格式 |
| @Pattern(regexp=) | String | 必须匹配指定的正则表达式 |
| @Valid | 对象 | 嵌套对象验证 |

### 常用正则表达式

| 用途 | 正则表达式 | 说明 |
|------|-----------|------|
| 手机号 | `^1[3-9]\\d{9}$` | 中国大陆手机号格式（1开头，第二位3-9，共11位） |
| 邮箱 | `^[A-Za-z0-9+_.-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,}$` | 标准邮箱格式 |
| 电话号码 | `^\\d{3,4}-?\\d{7,8}$` | 固定电话格式（如：010-12345678） |
| 银行账号 | `^[0-9]{10,30}$` | 10-30位数字 |
| 币种 | `^[A-Z]{3}$` | 3位大写字母（如：CNY、USD） |

### 前端实现建议

1. **客户端验证**：在发送请求前，前端应先进行基本的参数验证，减少无效请求
2. **错误处理**：统一处理API返回的错误信息，友好地展示给用户
3. **表单验证**：使用表单验证库（如VeeValidate、Formik等）实现实时验证
4. **错误提示**：根据错误信息中的字段名定位到对应的表单字段并显示错误
5. **重试机制**：对于频率限制错误，实现适当的重试机制

---

## 附录

### A. 日期时间格式

- **LocalDate**: `yyyy-MM-dd`（如：2024-01-01）
- **LocalDateTime**: `yyyy-MM-ddTHH:mm:ss`（如：2024-01-01T10:30:00）

### B. 数值格式

- **BigDecimal**: 使用字符串格式传输，避免精度丢失（如：1000000.00）
- **Integer/Long**: 标准整数格式（如：1、100）

### C. 布尔值格式

- **Boolean**: true/false
- **Integer**: 0/1（用于表示布尔状态的整数字段）

### D. 枚举值

系统中的枚举值通常使用大写字母和下划线分隔，如：
- 用户状态：ACTIVE、INACTIVE、LOCKED、DELETED
- 流水类型：INCOME、EXPENSE
- 审批状态：APPROVED、REJECTED
- 权限级别：VIEW、EDIT、ADMIN
- 执行状态：NOT_STARTED、IN_PROGRESS、COMPLETED、DELAYED、CANCELLED

---

## 更新记录

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| 1.0 | 2024-01-09 | 初始版本，包含所有新增API接口的参数校验规则 |

---

**文档结束**
