# 案件工作流程后端实现技术文档

## 文档信息

- **文档版本**: v1.0.0
- **创建日期**: 2026-01-19
- **适用系统**: 破产案件管理系统
- **基础URL**: `http://localhost:8080/api/v1`
- **目标读者**: 后端开发人员、系统架构师

---

## 目录

- [1. 功能需求与用户故事](#1-功能需求与用户故事)
- [2. 后端架构与数据库设计要求](#2-后端架构与数据库设计要求)
- [3. 后端实现概述](#3-后端实现概述)
- [4. 数据库架构设计](#4-数据库架构设计)
- [5. 示例初始数据集](#5-示例初始数据集)
- [6. 实施优先级与技术考量](#6-实施优先级与技术考量)

---

## 1. 功能需求与用户故事

### 1.1 功能需求概述

案件工作流程页面是案件详情模块的核心组成部分，旨在为用户提供案件全生命周期的可视化展示、状态跟踪和历史时间线查看功能。该模块需要支持破产案件从立案到终结的完整流程管理。

### 1.2 核心功能模块

#### 1.2.1 案件进展可视化展示

**功能描述**: 以时间轴或流程图形式展示案件当前所处阶段及整体进展情况。

**用户故事**:
- 作为一名管理人，我需要一眼就能看到案件当前所处的阶段，以便快速了解案件进展
- 作为一名系统管理员，我需要监控所有案件的工作流状态，以便进行统计和分析

**功能要求**:
- 支持横向时间轴展示，清晰标识已完成、进行中、未开始的阶段
- 支持阶段颜色编码（绿色-已完成，蓝色-进行中，灰色-未开始）
- 显示每个阶段的预计时间和实际完成时间
- 支持点击阶段查看详细信息
- 支持阶段之间的依赖关系展示

#### 1.2.2 状态跟踪与管理

**功能描述**: 实时跟踪案件状态变化，支持状态转换和审批流程。

**用户故事**:
- 作为一名法官，我需要能够推进案件到下一个阶段，并记录审批意见
- 作为一名管理人，我需要提交阶段完成申请，并上传相关证明材料
- 作为一名系统管理员，我需要能够回退案件到上一个阶段，处理异常情况
- 作为一名债权人，我需要能够查看案件状态变更历史，了解案件处理过程

**功能要求**:
- 支持案件状态的实时更新和同步
- 支持状态转换的业务规则验证
- 支持状态转换的审批流程
- 记录每次状态变更的操作人、操作时间、变更原因
- 支持状态变更通知（邮件、短信、系统消息）
- 支持状态变更的权限控制

#### 1.2.3 历史时间线查看

**功能描述**: 提供完整的案件操作历史记录，支持按时间、操作类型、操作人等多维度筛选。

**用户故事**:
- 作为一名法官，我需要查看案件的所有操作记录，以便进行审计和追溯
- 作为一名管理人，我需要查看自己在这个案件中的所有操作记录
- 作为一名系统管理员，我需要能够导出案件操作历史，用于合规检查
- 作为一名债权人，我需要查看案件的关键节点变更记录

**功能要求**:
- 按时间倒序展示所有操作记录
- 支持按操作类型筛选（状态变更、文件上传、会议记录等）
- 支持按操作人筛选
- 支持按时间范围筛选
- 显示操作的详细信息（操作人、操作时间、操作内容、关联文件等）
- 支持操作记录的导出功能（Excel、PDF）
- 支持关键操作的标记和备注

### 1.3 案件工作流阶段定义

根据破产案件管理的实际业务流程，定义以下工作流阶段：

| 阶段编号 | 阶段名称 | 阶段描述 | 预计时长 | 依赖阶段 |
|---------|---------|---------|---------|---------|
| 1 | 预重整/庭外重组 | 预重整或庭外重组阶段（可选） | 30-90天 | 无 |
| 2 | 立案受理 | 法院立案受理阶段 | 5-15天 | 无 |
| 3 | 债权申报 | 债权人申报债权阶段 | 30-60天 | 2 |
| 4 | 债权审查 | 管理人审查债权阶段 | 15-30天 | 3 |
| 5 | 财产调查 | 调查债务人财产阶段 | 30-60天 | 2 |
| 6 | 财产评估 | 对财产进行评估阶段 | 15-30天 | 5 |
| 7 | 第一次债权人会议 | 召开第一次债权人会议 | 3-7天 | 4,6 |
| 8 | 财产处置 | 处置破产财产阶段 | 60-180天 | 7 |
| 9 | 第二次债权人会议 | 召开第二次债权人会议 | 3-7天 | 8 |
| 10 | 分配方案执行 | 执行财产分配方案 | 30-90天 | 9 |
| 11 | 案件终结 | 案件终结与归档 | 15-30天 | 10 |

### 1.4 案件状态定义

| 状态代码 | 状态名称 | 状态描述 | 是否终态 |
|---------|---------|---------|---------|
| DRAFT | 草稿 | 案件信息录入中 | 否 |
| SUBMITTED | 已提交 | 案件已提交待审核 | 否 |
| UNDER_REVIEW | 审核中 | 案件正在审核 | 否 |
| APPROVED | 已批准 | 案件已批准进入正式流程 | 否 |
| IN_PROGRESS | 进行中 | 案件正在处理中 | 否 |
| SUSPENDED | 暂停 | 案件因特殊情况暂停 | 否 |
| COMPLETED | 已完成 | 案件正常完成 | 是 |
| TERMINATED | 已终止 | 案件被终止 | 是 |
| ARCHIVED | 已归档 | 案件已归档 | 是 |

### 1.5 用户角色与权限

| 角色代码 | 角色名称 | 角色描述 | 主要权限 |
|---------|---------|---------|---------|
| JUDGE | 法官 | 法院法官 | 查看案件、审批案件、推进阶段、查看所有记录 |
| ADMINISTRATOR | 管理人 | 破产管理人 | 管理案件、上传文件、提交申请、查看分配记录 |
| CREDITOR | 债权人 | 案件债权人 | 查看案件、申报债权、查看分配记录 |
| CLERK | 书记员 | 法院书记员 | 协助法官处理案件、录入信息 |
| ADMIN | 系统管理员 | 系统管理员 | 系统配置、用户管理、数据维护 |

---

## 2. 后端架构与数据库设计要求



## 3. 后端实现概述

### 3.1 核心 API 端点

#### 3.1.1 工作流管理 API

##### 3.1.1.1 获取案件工作流信息

**接口描述**: 获取指定案件的完整工作流信息，包括当前阶段、所有阶段状态、时间线等。

**请求信息**:
```
GET /api/v1/case-workflow/{caseId}
```

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| caseId | Long | 是 | 案件ID | 123 |

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "caseId": 123,
    "caseNo": "PC-2026-001",
    "caseName": "某某公司破产清算案",
    "currentStage": {
      "stageId": 5,
      "stageCode": "PROPERTY_INVESTIGATION",
      "stageName": "财产调查",
      "stageOrder": 5,
      "status": "IN_PROGRESS",
      "startTime": "2026-01-10 09:00:00",
      "estimatedEndTime": "2026-03-10 18:00:00",
      "progress": 45.5,
      "description": "正在调查债务人财产情况"
    },
    "stages": [
      {
        "stageId": 1,
        "stageCode": "PRE_REORGANIZATION",
        "stageName": "预重整/庭外重组",
        "stageOrder": 1,
        "status": "SKIPPED",
        "startTime": null,
        "endTime": null,
        "duration": null,
        "progress": 0,
        "isOptional": true
      },
      {
        "stageId": 2,
        "stageCode": "CASE_FILING",
        "stageName": "立案受理",
        "stageOrder": 2,
        "status": "COMPLETED",
        "startTime": "2026-01-01 09:00:00",
        "endTime": "2026-01-05 17:00:00",
        "duration": 5,
        "progress": 100,
        "isOptional": false
      },
      {
        "stageId": 3,
        "stageCode": "CLAIM_REGISTRATION",
        "stageName": "债权申报",
        "stageOrder": 3,
        "status": "COMPLETED",
        "startTime": "2026-01-06 09:00:00",
        "endTime": "2026-02-15 17:00:00",
        "duration": 40,
        "progress": 100,
        "isOptional": false
      },
      {
        "stageId": 4,
        "stageCode": "CLAIM_REVIEW",
        "stageName": "债权审查",
        "stageOrder": 4,
        "status": "COMPLETED",
        "startTime": "2026-02-16 09:00:00",
        "endTime": "2026-03-05 17:00:00",
        "duration": 18,
        "progress": 100,
        "isOptional": false
      },
      {
        "stageId": 5,
        "stageCode": "PROPERTY_INVESTIGATION",
        "stageName": "财产调查",
        "stageOrder": 5,
        "status": "IN_PROGRESS",
        "startTime": "2026-01-10 09:00:00",
        "endTime": null,
        "duration": null,
        "progress": 45.5,
        "isOptional": false
      }
    ],
    "overallProgress": 54.5,
    "estimatedCompletionDate": "2026-08-15",
    "workflowStatus": "IN_PROGRESS"
  }
}
```

##### 3.1.1.2 推进案件到下一阶段

**接口描述**: 将案件推进到下一个工作流阶段，需要验证业务规则和权限。

**请求信息**:
```
POST /api/v1/case-workflow/{caseId}/advance
Content-Type: application/json
```

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| caseId | Long | 是 | 案件ID | 123 |

**请求体**:
```json
{
  "targetStageId": 6,
  "reason": "财产调查已完成，准备进入财产评估阶段",
  "attachments": [
    {
      "fileId": 5001,
      "fileName": "财产调查报告.pdf",
      "fileType": "REPORT"
    }
  ],
  "comments": "财产调查已全部完成，共发现不动产3处，动产若干，银行账户5个"
}
```

**请求参数说明**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| targetStageId | Long | 是 | 目标阶段ID |
| reason | String | 是 | 推进原因 |
| attachments | Array | 否 | 附件列表 |
| comments | String | 否 | 备注信息 |

**响应示例**:
```json
{
  "code": 200,
  "message": "案件已成功推进到下一阶段",
  "data": {
    "caseId": 123,
    "previousStage": {
      "stageId": 5,
      "stageCode": "PROPERTY_INVESTIGATION",
      "stageName": "财产调查",
      "endTime": "2026-03-10 17:00:00",
      "status": "COMPLETED"
    },
    "currentStage": {
      "stageId": 6,
      "stageCode": "PROPERTY_EVALUATION",
      "stageName": "财产评估",
      "startTime": "2026-03-11 09:00:00",
      "status": "IN_PROGRESS"
    },
    "workflowTransitionId": 1001
  }
}
```

##### 3.1.1.3 回退案件到上一阶段

**接口描述**: 将案件回退到上一个工作流阶段，仅限特定角色操作。

**请求信息**:
```
POST /api/v1/case-workflow/{caseId}/rollback
Content-Type: application/json
```

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| caseId | Long | 是 | 案件ID | 123 |

**请求体**:
```json
{
  "targetStageId": 5,
  "reason": "财产评估报告存在问题，需要重新进行财产调查",
  "comments": "评估报告遗漏了部分财产，需要补充调查"
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "案件已成功回退到上一阶段",
  "data": {
    "caseId": 123,
    "previousStage": {
      "stageId": 6,
      "stageCode": "PROPERTY_EVALUATION",
      "stageName": "财产评估",
      "endTime": "2026-03-15 17:00:00",
      "status": "CANCELLED"
    },
    "currentStage": {
      "stageId": 5,
      "stageCode": "PROPERTY_INVESTIGATION",
      "stageName": "财产调查",
      "startTime": "2026-03-16 09:00:00",
      "status": "IN_PROGRESS"
    },
    "workflowTransitionId": 1002
  }
}
```

##### 3.1.1.4 暂停/恢复案件工作流

**接口描述**: 暂停或恢复案件的工作流推进。

**请求信息**:
```
POST /api/v1/case-workflow/{caseId}/suspend
Content-Type: application/json
```

**请求体**:
```json
{
  "action": "SUSPEND",
  "reason": "因债务人提出异议，需要暂停案件处理",
  "estimatedResumeDate": "2026-04-01"
}
```

**请求参数说明**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| action | String | 是 | 操作类型：SUSPEND/RESUME |
| reason | String | 是 | 原因 |
| estimatedResumeDate | String | 否 | 预计恢复日期（暂停时必填） |

**响应示例**:
```json
{
  "code": 200,
  "message": "案件已暂停",
  "data": {
    "caseId": 123,
    "workflowStatus": "SUSPENDED",
    "suspendTime": "2026-03-20 10:00:00",
    "estimatedResumeDate": "2026-04-01"
  }
}
```

#### 3.1.2 时间线与历史记录 API

##### 3.1.2.1 获取案件操作时间线

**接口描述**: 获取指定案件的操作时间线记录。

**请求信息**:
```
GET /api/v1/case-workflow/{caseId}/timeline
```

**路径参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| caseId | Long | 是 | 案件ID | 123 |

**查询参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| operationType | String | 否 | 操作类型 | STAGE_CHANGE |
| startTime | String | 否 | 开始时间 | 2026-01-01 00:00:00 |
| endTime | String | 否 | 结束时间 | 2026-03-31 23:59:59 |
| pageNum | Integer | 否 | 页码 | 1 |
| pageSize | Integer | 否 | 每页大小 | 20 |

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 156,
    "pageNum": 1,
    "pageSize": 20,
    "list": [
      {
        "timelineId": 1001,
        "caseId": 123,
        "operationType": "STAGE_CHANGE",
        "operationName": "阶段变更",
        "operationContent": "案件从【债权审查】阶段推进到【财产调查】阶段",
        "operatorId": 10,
        "operatorName": "张三",
        "operatorRole": "ADMINISTRATOR",
        "operationTime": "2026-03-10 14:30:00",
        "ipAddress": "192.168.1.100",
        "attachments": [
          {
            "fileId": 5001,
            "fileName": "债权审查报告.pdf",
            "fileUrl": "/api/v1/file/5001/download"
          }
        ],
        "metadata": {
          "previousStage": "CLAIM_REVIEW",
          "currentStage": "PROPERTY_INVESTIGATION",
          "transitionId": 1001
        }
      },
      {
        "timelineId": 1002,
        "caseId": 123,
        "operationType": "FILE_UPLOAD",
        "operationName": "文件上传",
        "operationContent": "上传了【财产调查报告】",
        "operatorId": 10,
        "operatorName": "张三",
        "operatorRole": "ADMINISTRATOR",
        "operationTime": "2026-03-09 16:45:00",
        "ipAddress": "192.168.1.100",
        "attachments": [
          {
            "fileId": 5002,
            "fileName": "财产调查报告.pdf",
            "fileUrl": "/api/v1/file/5002/download"
          }
        ],
        "metadata": {
          "fileId": 5002,
          "fileSize": 2048576,
          "fileCategory": "REPORT"
        }
      }
    ]
  }
}
```

##### 3.1.2.2 导出案件操作历史

**接口描述**: 导出案件的操作历史记录为Excel或PDF文件。

**请求信息**:
```
GET /api/v1/case-workflow/{caseId}/timeline/export
```

**查询参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| format | String | 否 | 导出格式：EXCEL/PDF | EXCEL |
| startTime | String | 否 | 开始时间 | 2026-01-01 00:00:00 |
| endTime | String | 否 | 结束时间 | 2026-03-31 23:59:59 |

**响应**: 文件流

#### 3.1.3 阶段管理 API

##### 3.1.3.1 获取工作流阶段配置

**接口描述**: 获取系统配置的所有工作流阶段信息。

**请求信息**:
```
GET /api/v1/workflow-stages
```

**查询参数**:
| 参数名 | 类型 | 必填 | 说明 | 示例值 |
|--------|------|------|------|--------|
| status | String | 否 | 状态 | ACTIVE |

**响应示例**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "stageId": 1,
      "stageCode": "PRE_REORGANIZATION",
      "stageName": "预重整/庭外重组",
      "stageOrder": 1,
      "isOptional": true,
      "estimatedDuration": 60,
      "description": "预重整或庭外重组阶段",
      "status": "ACTIVE",
      "dependencies": [],
      "requiredRoles": ["JUDGE", "ADMINISTRATOR"]
    },
    {
      "stageId": 2,
      "stageCode": "CASE_FILING",
      "stageName": "立案受理",
      "stageOrder": 2,
      "isOptional": false,
      "estimatedDuration": 10,
      "description": "法院立案受理阶段",
      "status": "ACTIVE",
      "dependencies": [],
      "requiredRoles": ["JUDGE", "CLERK"]
    },
    {
      "stageId": 3,
      "stageCode": "CLAIM_REGISTRATION",
      "stageName": "债权申报",
      "stageOrder": 3,
      "isOptional": false,
      "estimatedDuration": 45,
      "description": "债权人申报债权阶段",
      "status": "ACTIVE",
      "dependencies": [2],
      "requiredRoles": ["ADMINISTRATOR"]
    }
  ]
}
```

##### 3.1.3.2 更新阶段进度

**接口描述**: 更新当前阶段的进度信息。

**请求信息**:
```
PUT /api/v1/case-workflow/{caseId}/stage-progress
Content-Type: application/json
```

**请求体**:
```json
{
  "progress": 65.5,
  "comments": "已完成不动产调查，正在进行动产调查",
  "milestones": [
    {
      "milestoneName": "不动产调查",
      "status": "COMPLETED",
      "completionTime": "2026-02-28 17:00:00"
    },
    {
      "milestoneName": "动产调查",
      "status": "IN_PROGRESS",
      "completionTime": null
    }
  ]
}
```

**响应示例**:
```json
{
  "code": 200,
  "message": "阶段进度更新成功",
  "data": {
    "caseId": 123,
    "stageId": 5,
    "progress": 65.5,
    "updateTime": "2026-03-10 15:30:00"
  }
}
```

### 3.2 认证和授权

#### 3.2.1 认证机制

采用 JWT (JSON Web Token) 认证机制：

**认证流程**:
1. 用户使用用户名/密码登录
2. 后端验证用户凭证
3. 生成包含用户信息的 JWT Token
4. 客户端在后续请求中携带 Token
5. 后端验证 Token 有效性并提取用户信息

**Token 结构**:
```json
{
  "header": {
    "alg": "HS256",
    "typ": "JWT"
  },
  "payload": {
    "userId": 10,
    "username": "zhangsan",
    "roles": ["ADMINISTRATOR"],
    "caseIds": [123, 124, 125],
    "exp": 1737408000,
    "iat": 1737321600
  }
}
```

**Token 有效期**:
- Access Token: 2小时
- Refresh Token: 7天

#### 3.2.2 授权策略

基于角色的访问控制（RBAC）结合基于资源的访问控制：

**权限矩阵**:

| 操作 | JUDGE | ADMINISTRATOR | CREDITOR | CLERK | ADMIN |
|------|-------|---------------|----------|-------|------|
| 查看工作流 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 推进阶段 | ✓ | ✓ | ✗ | ✗ | ✓ |
| 回退阶段 | ✓ | ✗ | ✗ | ✗ | ✓ |
| 暂停/恢复 | ✓ | ✗ | ✗ | ✗ | ✓ |
| 更新进度 | ✗ | ✓ | ✗ | ✗ | ✓ |
| 查看时间线 | ✓ | ✓ | ✓ | ✓ | ✓ |
| 导出历史 | ✓ | ✓ | ✗ | ✓ | ✓ |

**权限注解示例**:
```java
@PreAuthorize("hasAnyRole('JUDGE', 'ADMINISTRATOR', 'ADMIN')")
@PostMapping("/advance")
public Result advanceWorkflow(@PathVariable Long caseId, @RequestBody AdvanceRequest request) {
    // ...
}

@PreAuthorize("hasRole('ADMIN')")
@PostMapping("/rollback")
public Result rollbackWorkflow(@PathVariable Long caseId, @RequestBody RollbackRequest request) {
    // ...
}
```

#### 3.2.3 数据权限

**案件级权限控制**:
- 法官：查看所有案件
- 管理人：仅查看自己负责的案件
- 债权人：仅查看自己参与的案件
- 书记员：查看分配给自己的案件

**实现方式**:
```java
@DataScope(
    alias = "cw",
    permission = @DataPermission(
        type = DataPermissionType.CUSTOM,
        expression = "@dataScopeService.hasCasePermission(#caseId)"
    )
)
public CaseWorkflow getWorkflow(@PathVariable Long caseId) {
    // ...
}
```

### 3.3 工作流状态转换业务逻辑

#### 3.3.1 状态转换规则

**正向推进规则**:
1. 检查当前阶段是否已完成
2. 检查目标阶段是否依赖的前置阶段已完成
3. 检查用户是否有推进权限
4. 验证必填任务是否完成
5. 创建阶段转换记录
6. 更新案件状态
7. 发送通知

**回退规则**:
1. 检查用户是否有回退权限（仅法官和管理员）
2. 检查目标阶段是否可回退
3. 验证回退原因
4. 创建阶段转换记录
5. 更新案件状态
6. 发送通知

**暂停/恢复规则**:
1. 检查用户是否有暂停权限
2. 检查案件是否可以暂停
3. 验证暂停原因
4. 更新案件状态
5. 发送通知

#### 3.3.2 业务逻辑伪代码

```java
public WorkflowTransition advanceWorkflow(Long caseId, Long targetStageId, String reason, Long userId) {
    // 1. 获取案件工作流信息
    CaseWorkflow workflow = workflowRepository.findByCaseId(caseId);
    
    // 2. 获取当前阶段
    WorkflowStage currentStage = workflow.getCurrentStage();
    
    // 3. 获取目标阶段
    WorkflowStage targetStage = stageRepository.findById(targetStageId);
    
    // 4. 验证状态转换规则
    validateTransition(currentStage, targetStage, userId);
    
    // 5. 检查必填任务
    checkRequiredTasks(workflow, currentStage);
    
    // 6. 完成当前阶段
    currentStage.setStatus(StageStatus.COMPLETED);
    currentStage.setEndTime(LocalDateTime.now());
    currentStage.setDuration(calculateDuration(currentStage));
    
    // 7. 启动目标阶段
    targetStage.setStatus(StageStatus.IN_PROGRESS);
    targetStage.setStartTime(LocalDateTime.now());
    
    // 8. 更新工作流状态
    workflow.setCurrentStage(targetStage);
    workflow.setOverallProgress(calculateOverallProgress(workflow));
    
    // 9. 创建转换记录
    WorkflowTransition transition = new WorkflowTransition();
    transition.setCaseId(caseId);
    transition.setFromStageId(currentStage.getStageId());
    transition.setToStageId(targetStage.getStageId());
    transition.setTransitionType(TransitionType.ADVANCE);
    transition.setReason(reason);
    transition.setOperatorId(userId);
    transition.setTransitionTime(LocalDateTime.now());
    
    // 10. 保存数据
    workflowRepository.save(workflow);
    transitionRepository.save(transition);
    
    // 11. 创建时间线记录
    timelineService.createTimelineRecord(workflow, transition);
    
    // 12. 发送通知
    notificationService.sendWorkflowNotification(workflow, transition);
    
    return transition;
}
```

### 3.4 错误处理和验证策略

#### 3.4.1 错误码设计

| 错误码 | 错误类型 | 说明 |
|--------|---------|------|
| 200 | 成功 | 请求成功 |
| 400 | 参数错误 | 请求参数不正确 |
| 401 | 未授权 | Token无效或过期 |
| 403 | 禁止访问 | 权限不足 |
| 404 | 资源不存在 | 请求的资源不存在 |
| 409 | 状态冲突 | 当前状态不允许该操作 |
| 422 | 业务规则错误 | 违反业务规则 |
| 500 | 服务器错误 | 服务器内部错误 |

#### 3.4.2 业务规则错误

| 错误码 | 错误信息 | 处理建议 |
|--------|---------|----------|
| WF_001 | 当前阶段未完成，无法推进 | 请完成当前阶段后再推进 |
| WF_002 | 目标阶段的前置阶段未完成 | 请先完成前置阶段 |
| WF_003 | 案件已暂停，无法推进 | 请先恢复案件 |
| WF_004 | 案件已完成，无法推进 | 案件已结束 |
| WF_005 | 无权限执行此操作 | 请联系管理员授权 |
| WF_006 | 必填任务未完成 | 请完成所有必填任务 |
| WF_007 | 阶段依赖关系不满足 | 请检查阶段依赖关系 |
| WF_008 | 回退阶段无效 | 不能回退到当前阶段或更早阶段 |

#### 3.4.3 参数验证

**请求体验证**:
```java
@Data
public class AdvanceRequest {
    @NotNull(message = "目标阶段ID不能为空")
    private Long targetStageId;
    
    @NotBlank(message = "推进原因不能为空")
    @Size(max = 500, message = "推进原因不能超过500字")
    private String reason;
    
    @Size(max = 1000, message = "备注不能超过1000字")
    private String comments;
    
    @Valid
    private List<Attachment> attachments;
}
```

**业务规则验证**:
```java
private void validateTransition(WorkflowStage currentStage, WorkflowStage targetStage, Long userId) {
    // 验证1: 检查当前阶段是否已完成
    if (currentStage.getStatus() != StageStatus.COMPLETED) {
        throw new BusinessException("WF_001", "当前阶段未完成，无法推进");
    }
    
    // 验证2: 检查目标阶段是否依赖的前置阶段已完成
    List<Long> dependencies = targetStage.getDependencies();
    for (Long depStageId : dependencies) {
        WorkflowStage depStage = workflow.getStageById(depStageId);
        if (depStage.getStatus() != StageStatus.COMPLETED) {
            throw new BusinessException("WF_002", 
                String.format("目标阶段的前置阶段【%s】未完成", depStage.getStageName()));
        }
    }
    
    // 验证3: 检查用户权限
    if (!hasPermission(userId, "ADVANCE_WORKFLOW")) {
        throw new BusinessException("WF_005", "无权限执行此操作");
    }
}
```

#### 3.4.4 异常处理

**全局异常处理器**:
```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(BusinessException.class)
    public Result handleBusinessException(BusinessException e) {
        log.error("业务异常: {}", e.getMessage());
        return Result.error(e.getErrorCode(), e.getMessage());
    }
    
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result handleValidationException(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldErrors().stream()
            .map(FieldError::getDefaultMessage)
            .collect(Collectors.joining(", "));
        return Result.error(400, message);
    }
    
    @ExceptionHandler(Exception.class)
    public Result handleException(Exception e) {
        log.error("系统异常", e);
        return Result.error(500, "系统繁忙，请稍后重试");
    }
}
```

---

## 4. 数据库架构设计

### 4.1 核心表结构

#### 4.1.1 案件工作流主表 (case_workflow)

**表描述**: 存储案件的工作流主信息。

```sql
CREATE TABLE `case_workflow` (
  `workflow_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '工作流ID',
  `case_id` BIGINT UNSIGNED NOT NULL COMMENT '案件ID',
  `case_no` VARCHAR(50) NOT NULL COMMENT '案件编号',
  `case_name` VARCHAR(200) NOT NULL COMMENT '案件名称',
  `current_stage_id` BIGINT UNSIGNED NOT NULL COMMENT '当前阶段ID',
  `workflow_status` VARCHAR(20) NOT NULL DEFAULT 'IN_PROGRESS' COMMENT '工作流状态: DRAFT-草稿, SUBMITTED-已提交, UNDER_REVIEW-审核中, APPROVED-已批准, IN_PROGRESS-进行中, SUSPENDED-暂停, COMPLETED-已完成, TERMINATED-已终止, ARCHIVED-已归档',
  `overall_progress` DECIMAL(5,2) DEFAULT 0.00 COMMENT '整体进度百分比',
  `estimated_start_date` DATE COMMENT '预计开始日期',
  `actual_start_date` DATE COMMENT '实际开始日期',
  `estimated_completion_date` DATE COMMENT '预计完成日期',
  `actual_completion_date` DATE COMMENT '实际完成日期',
  `total_duration` INT COMMENT '总持续天数',
  `suspend_count` INT DEFAULT 0 COMMENT '暂停次数',
  `suspend_duration` INT DEFAULT 0 COMMENT '暂停总天数',
  `created_by` BIGINT UNSIGNED NOT NULL COMMENT '创建人ID',
  `created_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_by` BIGINT UNSIGNED COMMENT '更新人ID',
  `updated_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT(1) DEFAULT 0 COMMENT '是否删除: 0-否, 1-是',
  `version` INT DEFAULT 1 COMMENT '版本号',
  PRIMARY KEY (`workflow_id`),
  UNIQUE KEY `uk_case_id` (`case_id`),
  UNIQUE KEY `uk_case_no` (`case_no`),
  KEY `idx_current_stage_id` (`current_stage_id`),
  KEY `idx_workflow_status` (`workflow_status`),
  KEY `idx_created_time` (`created_time`),
  CONSTRAINT `fk_case_workflow_created_by` FOREIGN KEY (`created_by`) REFERENCES `sys_user` (`user_id`),
  CONSTRAINT `fk_case_workflow_updated_by` FOREIGN KEY (`updated_by`) REFERENCES `sys_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='案件工作流主表';
```

#### 4.1.2 工作流阶段配置表 (workflow_stage_config)

**表描述**: 存储工作流阶段的配置信息。

```sql
CREATE TABLE `workflow_stage_config` (
  `stage_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '阶段ID',
  `stage_code` VARCHAR(50) NOT NULL COMMENT '阶段代码',
  `stage_name` VARCHAR(100) NOT NULL COMMENT '阶段名称',
  `stage_order` INT NOT NULL COMMENT '阶段顺序',
  `is_optional` TINYINT(1) DEFAULT 0 COMMENT '是否可选: 0-否, 1-是',
  `estimated_duration` INT COMMENT '预计持续天数',
  `description` VARCHAR(500) COMMENT '阶段描述',
  `required_roles` JSON COMMENT '所需角色列表',
  `dependencies` JSON COMMENT '依赖的阶段ID列表',
  `status` VARCHAR(20) NOT NULL DEFAULT 'ACTIVE' COMMENT '状态: ACTIVE-激活, INACTIVE-停用',
  `created_by` BIGINT UNSIGNED NOT NULL COMMENT '创建人ID',
  `created_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_by` BIGINT UNSIGNED COMMENT '更新人ID',
  `updated_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT(1) DEFAULT 0 COMMENT '是否删除: 0-否, 1-是',
  PRIMARY KEY (`stage_id`),
  UNIQUE KEY `uk_stage_code` (`stage_code`),
  KEY `idx_stage_order` (`stage_order`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='工作流阶段配置表';
```

#### 4.1.3 案件工作流阶段实例表 (case_workflow_stage)

**表描述**: 存储案件每个阶段的实例信息。

```sql
CREATE TABLE `case_workflow_stage` (
  `instance_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '实例ID',
  `workflow_id` BIGINT UNSIGNED NOT NULL COMMENT '工作流ID',
  `case_id` BIGINT UNSIGNED NOT NULL COMMENT '案件ID',
  `stage_id` BIGINT UNSIGNED NOT NULL COMMENT '阶段ID',
  `stage_code` VARCHAR(50) NOT NULL COMMENT '阶段代码',
  `stage_name` VARCHAR(100) NOT NULL COMMENT '阶段名称',
  `stage_order` INT NOT NULL COMMENT '阶段顺序',
  `stage_status` VARCHAR(20) NOT NULL DEFAULT 'PENDING' COMMENT '阶段状态: PENDING-待开始, IN_PROGRESS-进行中, COMPLETED-已完成, SKIPPED-跳过, CANCELLED-取消',
  `progress` DECIMAL(5,2) DEFAULT 0.00 COMMENT '阶段进度百分比',
  `start_time` DATETIME COMMENT '开始时间',
  `end_time` DATETIME COMMENT '结束时间',
  `duration` INT COMMENT '持续天数',
  `estimated_start_date` DATE COMMENT '预计开始日期',
  `estimated_end_date` DATE COMMENT '预计结束日期',
  `responsible_user_id` BIGINT UNSIGNED COMMENT '负责人ID',
  `responsible_user_name` VARCHAR(50) COMMENT '负责人姓名',
  `comments` TEXT COMMENT '备注',
  `milestones` JSON COMMENT '里程碑列表',
  `created_by` BIGINT UNSIGNED NOT NULL COMMENT '创建人ID',
  `created_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_by` BIGINT UNSIGNED COMMENT '更新人ID',
  `updated_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT(1) DEFAULT 0 COMMENT '是否删除: 0-否, 1-是',
  PRIMARY KEY (`instance_id`),
  UNIQUE KEY `uk_workflow_stage` (`workflow_id`, `stage_id`),
  KEY `idx_case_id` (`case_id`),
  KEY `idx_stage_id` (`stage_id`),
  KEY `idx_stage_status` (`stage_status`),
  KEY `idx_responsible_user_id` (`responsible_user_id`),
  CONSTRAINT `fk_case_workflow_stage_workflow_id` FOREIGN KEY (`workflow_id`) REFERENCES `case_workflow` (`workflow_id`),
  CONSTRAINT `fk_case_workflow_stage_stage_id` FOREIGN KEY (`stage_id`) REFERENCES `workflow_stage_config` (`stage_id`),
  CONSTRAINT `fk_case_workflow_stage_responsible_user_id` FOREIGN KEY (`responsible_user_id`) REFERENCES `sys_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='案件工作流阶段实例表';
```

#### 4.1.4 工作流转换记录表 (workflow_transition)

**表描述**: 记录工作流阶段的转换历史。

```sql
CREATE TABLE `workflow_transition` (
  `transition_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '转换ID',
  `workflow_id` BIGINT UNSIGNED NOT NULL COMMENT '工作流ID',
  `case_id` BIGINT UNSIGNED NOT NULL COMMENT '案件ID',
  `from_stage_id` BIGINT UNSIGNED COMMENT '源阶段ID',
  `from_stage_name` VARCHAR(100) COMMENT '源阶段名称',
  `to_stage_id` BIGINT UNSIGNED COMMENT '目标阶段ID',
  `to_stage_name` VARCHAR(100) COMMENT '目标阶段名称',
  `transition_type` VARCHAR(20) NOT NULL COMMENT '转换类型: ADVANCE-推进, ROLLBACK-回退, SUSPEND-暂停, RESUME-恢复, SKIP-跳过',
  `reason` VARCHAR(500) NOT NULL COMMENT '转换原因',
  `comments` TEXT COMMENT '备注',
  `operator_id` BIGINT UNSIGNED NOT NULL COMMENT '操作人ID',
  `operator_name` VARCHAR(50) NOT NULL COMMENT '操作人姓名',
  `operator_role` VARCHAR(50) COMMENT '操作人角色',
  `transition_time` DATETIME NOT NULL COMMENT '转换时间',
  `ip_address` VARCHAR(50) COMMENT 'IP地址',
  `attachments` JSON COMMENT '附件列表',
  `metadata` JSON COMMENT '元数据',
  `created_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`transition_id`),
  KEY `idx_workflow_id` (`workflow_id`),
  KEY `idx_case_id` (`case_id`),
  KEY `idx_from_stage_id` (`from_stage_id`),
  KEY `idx_to_stage_id` (`to_stage_id`),
  KEY `idx_operator_id` (`operator_id`),
  KEY `idx_transition_time` (`transition_time`),
  CONSTRAINT `fk_workflow_transition_workflow_id` FOREIGN KEY (`workflow_id`) REFERENCES `case_workflow` (`workflow_id`),
  CONSTRAINT `fk_workflow_transition_operator_id` FOREIGN KEY (`operator_id`) REFERENCES `sys_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='工作流转换记录表';
```

#### 4.1.5 工作流时间线表 (workflow_timeline)

**表描述**: 记录案件的所有操作历史。

```sql
CREATE TABLE `workflow_timeline` (
  `timeline_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '时间线ID',
  `case_id` BIGINT UNSIGNED NOT NULL COMMENT '案件ID',
  `workflow_id` BIGINT UNSIGNED NOT NULL COMMENT '工作流ID',
  `operation_type` VARCHAR(50) NOT NULL COMMENT '操作类型: STAGE_CHANGE-阶段变更, FILE_UPLOAD-文件上传, MEETING_CREATE-会议创建, TASK_COMPLETE-任务完成, STATUS_CHANGE-状态变更, OTHER-其他',
  `operation_name` VARCHAR(100) NOT NULL COMMENT '操作名称',
  `operation_content` TEXT NOT NULL COMMENT '操作内容',
  `operator_id` BIGINT UNSIGNED NOT NULL COMMENT '操作人ID',
  `operator_name` VARCHAR(50) NOT NULL COMMENT '操作人姓名',
  `operator_role` VARCHAR(50) COMMENT '操作人角色',
  `operation_time` DATETIME NOT NULL COMMENT '操作时间',
  `ip_address` VARCHAR(50) COMMENT 'IP地址',
  `attachments` JSON COMMENT '附件列表',
  `metadata` JSON COMMENT '元数据',
  `is_key_event` TINYINT(1) DEFAULT 0 COMMENT '是否关键事件: 0-否, 1-是',
  `created_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`timeline_id`),
  KEY `idx_case_id` (`case_id`),
  KEY `idx_workflow_id` (`workflow_id`),
  KEY `idx_operation_type` (`operation_type`),
  KEY `idx_operator_id` (`operator_id`),
  KEY `idx_operation_time` (`operation_time`),
  KEY `idx_is_key_event` (`is_key_event`),
  CONSTRAINT `fk_workflow_timeline_case_id` FOREIGN KEY (`case_id`) REFERENCES `case_workflow` (`case_id`),
  CONSTRAINT `fk_workflow_timeline_operator_id` FOREIGN KEY (`operator_id`) REFERENCES `sys_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='工作流时间线表';
```

#### 4.1.6 阶段任务表 (stage_task)

**表描述**: 定义每个阶段的任务列表。

```sql
CREATE TABLE `stage_task` (
  `task_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '任务ID',
  `stage_id` BIGINT UNSIGNED NOT NULL COMMENT '阶段ID',
  `task_code` VARCHAR(50) NOT NULL COMMENT '任务代码',
  `task_name` VARCHAR(100) NOT NULL COMMENT '任务名称',
  `task_order` INT NOT NULL COMMENT '任务顺序',
  `is_required` TINYINT(1) DEFAULT 1 COMMENT '是否必填: 0-否, 1-是',
  `description` VARCHAR(500) COMMENT '任务描述',
  `required_roles` JSON COMMENT '所需角色列表',
  `estimated_duration` INT COMMENT '预计持续小时数',
  `status` VARCHAR(20) NOT NULL DEFAULT 'ACTIVE' COMMENT '状态: ACTIVE-激活, INACTIVE-停用',
  `created_by` BIGINT UNSIGNED NOT NULL COMMENT '创建人ID',
  `created_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_by` BIGINT UNSIGNED COMMENT '更新人ID',
  `updated_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT(1) DEFAULT 0 COMMENT '是否删除: 0-否, 1-是',
  PRIMARY KEY (`task_id`),
  UNIQUE KEY `uk_stage_task` (`stage_id`, `task_code`),
  KEY `idx_task_code` (`task_code`),
  KEY `idx_status` (`status`),
  CONSTRAINT `fk_stage_task_stage_id` FOREIGN KEY (`stage_id`) REFERENCES `workflow_stage_config` (`stage_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='阶段任务表';
```

#### 4.1.7 案件阶段任务实例表 (case_stage_task)

**表描述**: 记录案件每个阶段任务的完成情况。

```sql
CREATE TABLE `case_stage_task` (
  `task_instance_id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '任务实例ID',
  `case_id` BIGINT UNSIGNED NOT NULL COMMENT '案件ID',
  `workflow_id` BIGINT UNSIGNED NOT NULL COMMENT '工作流ID',
  `stage_instance_id` BIGINT UNSIGNED NOT NULL COMMENT '阶段实例ID',
  `task_id` BIGINT UNSIGNED NOT NULL COMMENT '任务ID',
  `task_code` VARCHAR(50) NOT NULL COMMENT '任务代码',
  `task_name` VARCHAR(100) NOT NULL COMMENT '任务名称',
  `task_status` VARCHAR(20) NOT NULL DEFAULT 'PENDING' COMMENT '任务状态: PENDING-待完成, IN_PROGRESS-进行中, COMPLETED-已完成, SKIPPED-跳过',
  `start_time` DATETIME COMMENT '开始时间',
  `end_time` DATETIME COMMENT '完成时间',
  `duration` INT COMMENT '持续小时数',
  `responsible_user_id` BIGINT UNSIGNED COMMENT '负责人ID',
  `responsible_user_name` VARCHAR(50) COMMENT '负责人姓名',
  `completion_notes` TEXT COMMENT '完成说明',
  `attachments` JSON COMMENT '附件列表',
  `created_by` BIGINT UNSIGNED NOT NULL COMMENT '创建人ID',
  `created_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_by` BIGINT UNSIGNED COMMENT '更新人ID',
  `updated_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `deleted` TINYINT(1) DEFAULT 0 COMMENT '是否删除: 0-否, 1-是',
  PRIMARY KEY (`task_instance_id`),
  UNIQUE KEY `uk_case_task` (`case_id`, `task_id`),
  KEY `idx_workflow_id` (`workflow_id`),
  KEY `idx_stage_instance_id` (`stage_instance_id`),
  KEY `idx_task_id` (`task_id`),
  KEY `idx_task_status` (`task_status`),
  KEY `idx_responsible_user_id` (`responsible_user_id`),
  CONSTRAINT `fk_case_stage_task_case_id` FOREIGN KEY (`case_id`) REFERENCES `case_workflow` (`case_id`),
  CONSTRAINT `fk_case_stage_task_workflow_id` FOREIGN KEY (`workflow_id`) REFERENCES `case_workflow` (`workflow_id`),
  CONSTRAINT `fk_case_stage_task_stage_instance_id` FOREIGN KEY (`stage_instance_id`) REFERENCES `case_workflow_stage` (`instance_id`),
  CONSTRAINT `fk_case_stage_task_task_id` FOREIGN KEY (`task_id`) REFERENCES `stage_task` (`task_id`),
  CONSTRAINT `fk_case_stage_task_responsible_user_id` FOREIGN KEY (`responsible_user_id`) REFERENCES `sys_user` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='案件阶段任务实例表';
```

### 4.2 索引策略

#### 4.2.1 主键索引

所有表都使用自增主键，确保数据插入性能。

#### 4.2.2 唯一索引

- `case_workflow`: `case_id`, `case_no`
- `workflow_stage_config`: `stage_code`
- `case_workflow_stage`: `workflow_id`, `stage_id`
- `stage_task`: `stage_id`, `task_code`
- `case_stage_task`: `case_id`, `task_id`

#### 4.2.3 普通索引

**查询频繁字段**:
- `case_workflow`: `current_stage_id`, `workflow_status`, `created_time`
- `workflow_stage_config`: `stage_order`, `status`
- `case_workflow_stage`: `case_id`, `stage_id`, `stage_status`, `responsible_user_id`
- `workflow_transition`: `workflow_id`, `case_id`, `from_stage_id`, `to_stage_id`, `operator_id`, `transition_time`
- `workflow_timeline`: `case_id`, `workflow_id`, `operation_type`, `operator_id`, `operation_time`, `is_key_event`
- `stage_task`: `task_code`, `status`
- `case_stage_task`: `workflow_id`, `stage_instance_id`, `task_id`, `task_status`, `responsible_user_id`

#### 4.2.4 复合索引

**常用查询组合**:
- `workflow_timeline`: `(case_id, operation_time)` - 按案件和时间查询
- `workflow_transition`: `(case_id, transition_time)` - 按案件和时间查询
- `case_workflow_stage`: `(case_id, stage_status)` - 按案件和状态查询

### 4.3 外键关系与级联规则

#### 4.3.1 外键关系图

```
sys_user (用户表)
    ↓
case_workflow (工作流主表)
    ↓
case_workflow_stage (阶段实例表)
    ↓
case_stage_task (任务实例表)

workflow_stage_config (阶段配置表)
    ↓
stage_task (任务表)

case_workflow (工作流主表)
    ↓
workflow_transition (转换记录表)
    ↓
workflow_timeline (时间线表)
```

#### 4.3.2 级联规则

**更新规则**:
- 所有外键使用 `ON UPDATE CASCADE`，确保主表更新时子表同步更新

**删除规则**:
- `case_workflow`: 使用逻辑删除，不物理删除
- `workflow_stage_config`: 使用逻辑删除，不物理删除
- `stage_task`: 使用逻辑删除，不物理删除
- `case_workflow_stage`: 使用逻辑删除，不物理删除
- `workflow_transition`: 不允许删除，保留历史记录
- `workflow_timeline`: 不允许删除，保留历史记录
- `case_stage_task`: 使用逻辑删除，不物理删除

### 4.4 数据完整性约束

#### 4.4.1 检查约束

```sql
-- 工作流状态检查
ALTER TABLE `case_workflow` 
ADD CONSTRAINT `chk_workflow_status` 
CHECK (`workflow_status` IN ('DRAFT', 'SUBMITTED', 'UNDER_REVIEW', 'APPROVED', 'IN_PROGRESS', 'SUSPENDED', 'COMPLETED', 'TERMINATED', 'ARCHIVED'));

-- 阶段状态检查
ALTER TABLE `case_workflow_stage` 
ADD CONSTRAINT `chk_stage_status` 
CHECK (`stage_status` IN ('PENDING', 'IN_PROGRESS', 'COMPLETED', 'SKIPPED', 'CANCELLED'));

-- 进度范围检查
ALTER TABLE `case_workflow` 
ADD CONSTRAINT `chk_overall_progress` 
CHECK (`overall_progress` >= 0 AND `overall_progress` <= 100);

ALTER TABLE `case_workflow_stage` 
ADD CONSTRAINT `chk_stage_progress` 
CHECK (`progress` >= 0 AND `progress` <= 100);

-- 转换类型检查
ALTER TABLE `workflow_transition` 
ADD CONSTRAINT `chk_transition_type` 
CHECK (`transition_type` IN ('ADVANCE', 'ROLLBACK', 'SUSPEND', 'RESUME', 'SKIP'));

-- 任务状态检查
ALTER TABLE `case_stage_task` 
ADD CONSTRAINT `chk_task_status` 
CHECK (`task_status` IN ('PENDING', 'IN_PROGRESS', 'COMPLETED', 'SKIPPED'));
```

#### 4.4.2 触发器

```sql
-- 自动更新工作流整体进度触发器
DELIMITER $$
CREATE TRIGGER `trg_update_workflow_progress` 
AFTER UPDATE ON `case_workflow_stage`
FOR EACH ROW
BEGIN
    UPDATE `case_workflow` 
    SET `overall_progress` = (
        SELECT AVG(`progress`) 
        FROM `case_workflow_stage` 
        WHERE `workflow_id` = NEW.`workflow_id`
          AND `stage_status` IN ('IN_PROGRESS', 'COMPLETED')
    )
    WHERE `workflow_id` = NEW.`workflow_id`;
END$$
DELIMITER ;

-- 自动创建时间线记录触发器
DELIMITER $$
CREATE TRIGGER `trg_create_timeline_on_transition` 
AFTER INSERT ON `workflow_transition`
FOR EACH ROW
BEGIN
    INSERT INTO `workflow_timeline` (
        `case_id`, `workflow_id`, `operation_type`, `operation_name`, 
        `operation_content`, `operator_id`, `operator_name`, 
        `operator_role`, `operation_time`, `ip_address`, 
        `attachments`, `metadata`, `is_key_event`
    ) VALUES (
        NEW.`case_id`, 
        NEW.`workflow_id`, 
        'STAGE_CHANGE', 
        '阶段变更', 
        CONCAT('案件从【', NEW.`from_stage_name`, '】阶段', 
               CASE NEW.`transition_type` 
                   WHEN 'ADVANCE' THEN '推进到'
                   WHEN 'ROLLBACK' THEN '回退到'
                   WHEN 'SUSPEND' THEN '暂停'
                   WHEN 'RESUME' THEN '恢复'
                   ELSE '转换到'
               END, 
               '【', NEW.`to_stage_name`, '】阶段'),
        NEW.`operator_id`, 
        NEW.`operator_name`, 
        NEW.`operator_role`, 
        NEW.`transition_time`, 
        NEW.`ip_address`, 
        NEW.`attachments`, 
        NEW.`metadata`, 
        1
    );
END$$
DELIMITER ;
```

---

## 5. 示例初始数据集

### 5.1 工作流阶段配置数据

```sql
INSERT INTO `workflow_stage_config` 
(`stage_code`, `stage_name`, `stage_order`, `is_optional`, `estimated_duration`, `description`, `required_roles`, `dependencies`, `status`, `created_by`) 
VALUES
('PRE_REORGANIZATION', '预重整/庭外重组', 1, 1, 60, '预重整或庭外重组阶段（可选）', '["JUDGE", "ADMINISTRATOR"]', '[]', 'ACTIVE', 1),
('CASE_FILING', '立案受理', 2, 0, 10, '法院立案受理阶段', '["JUDGE", "CLERK"]', '[]', 'ACTIVE', 1),
('CLAIM_REGISTRATION', '债权申报', 3, 0, 45, '债权人申报债权阶段', '["ADMINISTRATOR"]', '[2]', 'ACTIVE', 1),
('CLAIM_REVIEW', '债权审查', 4, 0, 20, '管理人审查债权阶段', '["ADMINISTRATOR"]', '[3]', 'ACTIVE', 1),
('PROPERTY_INVESTIGATION', '财产调查', 5, 0, 45, '调查债务人财产阶段', '["ADMINISTRATOR"]', '[2]', 'ACTIVE', 1),
('PROPERTY_EVALUATION', '财产评估', 6, 0, 20, '对财产进行评估阶段', '["ADMINISTRATOR"]', '[5]', 'ACTIVE', 1),
('FIRST_CREDITORS_MEETING', '第一次债权人会议', 7, 0, 5, '召开第一次债权人会议', '["JUDGE", "ADMINISTRATOR"]', '[4,6]', 'ACTIVE', 1),
('PROPERTY_DISPOSAL', '财产处置', 8, 0, 120, '处置破产财产阶段', '["ADMINISTRATOR"]', '[7]', 'ACTIVE', 1),
('SECOND_CREDITORS_MEETING', '第二次债权人会议', 9, 0, 5, '召开第二次债权人会议', '["JUDGE", "ADMINISTRATOR"]', '[8]', 'ACTIVE', 1),
('DISTRIBUTION_EXECUTION', '分配方案执行', 10, 0, 60, '执行财产分配方案', '["ADMINISTRATOR"]', '[9]', 'ACTIVE', 1),
('CASE_TERMINATION', '案件终结', 11, 0, 20, '案件终结与归档', '["JUDGE", "ADMINISTRATOR"]', '[10]', 'ACTIVE', 1);
```

### 5.2 阶段任务配置数据

```sql
INSERT INTO `stage_task` 
(`stage_id`, `task_code`, `task_name`, `task_order`, `is_required`, `description`, `required_roles`, `estimated_duration`, `status`, `created_by`) 
VALUES
-- 立案受理阶段任务
(2, 'FILING_APPLICATION', '提交破产申请书', 1, 1, '债务人或债权人提交破产申请书', '["JUDGE", "CLERK"]', 2, 'ACTIVE', 1),
(2, 'COURT_ACCEPTANCE', '法院受理裁定', 2, 1, '法院作出受理裁定', '["JUDGE"]', 1, 'ACTIVE', 1),
(2, 'APPOINT_ADMINISTRATOR', '指定管理人', 3, 1, '法院指定破产管理人', '["JUDGE"]', 1, 'ACTIVE', 1),
(2, 'PUBLISH_NOTICE', '发布受理公告', 4, 1, '发布破产案件受理公告', '["CLERK"]', 1, 'ACTIVE', 1),

-- 债权申报阶段任务
(3, 'PUBLISH_CLAIM_NOTICE', '发布债权申报公告', 1, 1, '发布债权申报公告', '["ADMINISTRATOR"]', 1, 'ACTIVE', 1),
(3, 'REGISTER_CLAIMS', '登记债权申报', 2, 1, '登记债权人申报的债权', '["ADMINISTRATOR"]', 30, 'ACTIVE', 1),
(3, 'COLLECT_CLAIM_MATERIALS', '收集债权材料', 3, 1, '收集和整理债权申报材料', '["ADMINISTRATOR"]', 10, 'ACTIVE', 1),

-- 债权审查阶段任务
(4, 'REVIEW_CLAIMS', '审查债权', 1, 1, '对申报的债权进行审查', '["ADMINISTRATOR"]', 15, 'ACTIVE', 1),
(4, 'PREPARE_REVIEW_REPORT', '准备债权审查报告', 2, 1, '编制债权审查报告', '["ADMINISTRATOR"]', 3, 'ACTIVE', 1),
(4, 'CONFIRM_CLAIMS', '确认债权', 3, 1, '确认无争议债权', '["ADMINISTRATOR"]', 2, 'ACTIVE', 1),

-- 财产调查阶段任务
(5, 'INVESTIGATE_PROPERTY', '调查财产', 1, 1, '调查债务人财产情况', '["ADMINISTRATOR"]', 30, 'ACTIVE', 1),
(5, 'PREPARE_PROPERTY_LIST', '编制财产清单', 2, 1, '编制债务人财产清单', '["ADMINISTRATOR"]', 5, 'ACTIVE', 1),
(5, 'SEAL_PROPERTY', '查封财产', 3, 1, '对债务人财产进行查封', '["ADMINISTRATOR"]', 10, 'ACTIVE', 1),

-- 财产评估阶段任务
(6, 'HIRE_EVALUATOR', '聘请评估机构', 1, 1, '聘请专业评估机构', '["ADMINISTRATOR"]', 3, 'ACTIVE', 1),
(6, 'EVALUATE_PROPERTY', '评估财产价值', 2, 1, '对财产进行评估', '["ADMINISTRATOR"]', 10, 'ACTIVE', 1),
(6, 'PREPARE_EVALUATION_REPORT', '编制评估报告', 3, 1, '编制财产评估报告', '["ADMINISTRATOR"]', 5, 'ACTIVE', 1),

-- 第一次债权人会议阶段任务
(7, 'PREPARE_MEETING', '准备会议材料', 1, 1, '准备债权人会议材料', '["ADMINISTRATOR"]', 2, 'ACTIVE', 1),
(7, 'NOTIFY_MEETING', '通知参会人员', 2, 1, '通知债权人参加会议', '["ADMINISTRATOR"]', 1, 'ACTIVE', 1),
(7, 'CONDUCT_MEETING', '召开会议', 3, 1, '召开债权人会议', '["JUDGE", "ADMINISTRATOR"]', 1, 'ACTIVE', 1),
(7, 'PREPARE_MEETING_MINUTES', '编制会议纪要', 4, 1, '编制会议纪要', '["ADMINISTRATOR"]', 1, 'ACTIVE', 1),

-- 财产处置阶段任务
(8, 'PREPARE_DISPOSAL_PLAN', '制定处置方案', 1, 1, '制定财产处置方案', '["ADMINISTRATOR"]', 5, 'ACTIVE', 1),
(8, 'PUBLISH_DISPOSAL_NOTICE', '发布处置公告', 2, 1, '发布财产处置公告', '["ADMINISTRATOR"]', 1, 'ACTIVE', 1),
(8, 'EXECUTE_DISPOSAL', '执行处置', 3, 1, '执行财产处置', '["ADMINISTRATOR"]', 100, 'ACTIVE', 1),
(8, 'PREPARE_DISPOSAL_REPORT', '编制处置报告', 4, 1, '编制财产处置报告', '["ADMINISTRATOR"]', 10, 'ACTIVE', 1),

-- 第二次债权人会议阶段任务
(9, 'PREPARE_DISTRIBUTION_PLAN', '准备分配方案', 1, 1, '准备财产分配方案', '["ADMINISTRATOR"]', 5, 'ACTIVE', 1),
(9, 'NOTIFY_MEETING', '通知参会人员', 2, 1, '通知债权人参加会议', '["ADMINISTRATOR"]', 1, 'ACTIVE', 1),
(9, 'CONDUCT_MEETING', '召开会议', 3, 1, '召开债权人会议', '["JUDGE", "ADMINISTRATOR"]', 1, 'ACTIVE', 1),
(9, 'APPROVE_DISTRIBUTION_PLAN', '批准分配方案', 4, 1, '债权人会议批准分配方案', '["JUDGE"]', 1, 'ACTIVE', 1),

-- 分配方案执行阶段任务
(10, 'EXECUTE_DISTRIBUTION', '执行分配', 1, 1, '执行财产分配', '["ADMINISTRATOR"]', 30, 'ACTIVE', 1),
(10, 'PREPARE_DISTRIBUTION_REPORT', '编制分配报告', 2, 1, '编制财产分配报告', '["ADMINISTRATOR"]', 5, 'ACTIVE', 1),

-- 案件终结阶段任务
(11, 'PREPARE_TERMINATION_REPORT', '准备终结报告', 1, 1, '准备案件终结报告', '["ADMINISTRATOR"]', 5, 'ACTIVE', 1),
(11, 'APPLY_TERMINATION', '申请终结', 2, 1, '向法院申请终结破产程序', '["ADMINISTRATOR"]', 1, 'ACTIVE', 1),
(11, 'COURT_TERMINATION', '法院裁定终结', 3, 1, '法院裁定终结破产程序', '["JUDGE"]', 1, 'ACTIVE', 1),
(11, 'ARCHIVE_CASE', '案件归档', 4, 1, '整理并归档案件材料', '["ADMINISTRATOR"]', 10, 'ACTIVE', 1);
```

### 5.3 示例案件工作流数据

```sql
-- 插入示例案件工作流
INSERT INTO `case_workflow` 
(`case_id`, `case_no`, `case_name`, `current_stage_id`, `workflow_status`, `overall_progress`, `estimated_start_date`, `actual_start_date`, `estimated_completion_date`, `created_by`) 
VALUES
(123, 'PC-2026-001', '某某科技有限公司破产清算案', 5, 'IN_PROGRESS', 45.50, '2026-01-01', '2026-01-01', '2026-08-15', 1),
(124, 'PC-2026-002', '某某商贸有限公司破产重整案', 3, 'IN_PROGRESS', 27.27, '2026-01-10', '2026-01-10', '2026-09-30', 1),
(125, 'PC-2026-003', '某某制造企业破产清算案', 2, 'IN_PROGRESS', 18.18, '2026-01-15', '2026-01-15', '2026-07-20', 1);

-- 插入案件阶段实例（案件123）
INSERT INTO `case_workflow_stage` 
(`workflow_id`, `case_id`, `stage_id`, `stage_code`, `stage_name`, `stage_order`, `stage_status`, `progress`, `start_time`, `end_time`, `duration`, `responsible_user_id`, `responsible_user_name`) 
VALUES
-- 案件123的阶段实例
(1, 123, 1, 'PRE_REORGANIZATION', '预重整/庭外重组', 1, 'SKIPPED', 0, NULL, NULL, NULL, NULL, NULL),
(1, 123, 2, 'CASE_FILING', '立案受理', 2, 'COMPLETED', 100, '2026-01-01 09:00:00', '2026-01-05 17:00:00', 5, 2, '李法官'),
(1, 123, 3, 'CLAIM_REGISTRATION', '债权申报', 3, 'COMPLETED', 100, '2026-01-06 09:00:00', '2026-02-15 17:00:00', 40, 3, '张管理员'),
(1, 123, 4, 'CLAIM_REVIEW', '债权审查', 4, 'COMPLETED', 100, '2026-02-16 09:00:00', '2026-03-05 17:00:00', 18, 3, '张管理员'),
(1, 123, 5, 'PROPERTY_INVESTIGATION', '财产调查', 5, 'IN_PROGRESS', 45.5, '2026-03-06 09:00:00', NULL, NULL, 3, '张管理员'),
(1, 123, 6, 'PROPERTY_EVALUATION', '财产评估', 6, 'PENDING', 0, NULL, NULL, NULL, NULL, NULL),
(1, 123, 7, 'FIRST_CREDITORS_MEETING', '第一次债权人会议', 7, 'PENDING', 0, NULL, NULL, NULL, NULL, NULL),
(1, 123, 8, 'PROPERTY_DISPOSAL', '财产处置', 8, 'PENDING', 0, NULL, NULL, NULL, NULL, NULL),
(1, 123, 9, 'SECOND_CREDITORS_MEETING', '第二次债权人会议', 9, 'PENDING', 0, NULL, NULL, NULL, NULL, NULL),
(1, 123, 10, 'DISTRIBUTION_EXECUTION', '分配方案执行', 10, 'PENDING', 0, NULL, NULL, NULL, NULL, NULL),
(1, 123, 11, 'CASE_TERMINATION', '案件终结', 11, 'PENDING', 0, NULL, NULL, NULL, NULL, NULL);
```

### 5.4 工作流转换记录示例

```sql
INSERT INTO `workflow_transition` 
(`workflow_id`, `case_id`, `from_stage_id`, `from_stage_name`, `to_stage_id`, `to_stage_name`, `transition_type`, `reason`, `comments`, `operator_id`, `operator_name`, `operator_role`, `transition_time`, `ip_address`, `attachments`, `metadata`) 
VALUES
-- 案件123的转换记录
(1, 123, 2, '立案受理', 3, '债权申报', 'ADVANCE', '立案受理已完成，进入债权申报阶段', '已完成立案受理程序', 2, '李法官', 'JUDGE', '2026-01-05 17:30:00', '192.168.1.100', '[]', '{"previousStageId": 2, "currentStageId": 3}'),
(1, 123, 3, '债权申报', 4, '债权审查', 'ADVANCE', '债权申报已截止，进入债权审查阶段', '共收到债权申报15份', 3, '张管理员', 'ADMINISTRATOR', '2026-02-15 18:00:00', '192.168.1.101', '[{"fileId": 5001, "fileName": "债权申报登记表.pdf"}]', '{"previousStageId": 3, "currentStageId": 4, "claimCount": 15}'),
(1, 123, 4, '债权审查', 5, '财产调查', 'ADVANCE', '债权审查已完成，进入财产调查阶段', '确认无争议债权12笔，有争议债权3笔', 3, '张管理员', 'ADMINISTRATOR', '2026-03-05 17:30:00', '192.168.1.101', '[{"fileId": 5002, "fileName": "债权审查报告.pdf"}]', '{"previousStageId": 4, "currentStageId": 5, "confirmedClaims": 12, "disputedClaims": 3}');
```

### 5.5 时间线记录示例

```sql
INSERT INTO `workflow_timeline` 
(`case_id`, `workflow_id`, `operation_type`, `operation_name`, `operation_content`, `operator_id`, `operator_name`, `operator_role`, `operation_time`, `ip_address`, `attachments`, `metadata`, `is_key_event`) 
VALUES
-- 案件123的时间线记录
(123, 1, 'STAGE_CHANGE', '阶段变更', '案件从【立案受理】阶段推进到【债权申报】阶段', 2, '李法官', 'JUDGE', '2026-01-05 17:30:00', '192.168.1.100', '[]', '{"transitionId": 1, "fromStage": "CASE_FILING", "toStage": "CLAIM_REGISTRATION"}', 1),
(123, 1, 'FILE_UPLOAD', '文件上传', '上传了【债权申报公告】', 3, '张管理员', 'ADMINISTRATOR', '2026-01-06 10:00:00', '192.168.1.101', '[{"fileId": 5003, "fileName": "债权申报公告.pdf"}]', '{"fileId": 5003, "fileSize": 1048576, "fileCategory": "NOTICE"}', 1),
(123, 1, 'TASK_COMPLETE', '任务完成', '完成了【发布债权申报公告】任务', 3, '张管理员', 'ADMINISTRATOR', '2026-01-06 10:30:00', '192.168.1.101', '[]', '{"taskId": 101, "taskCode": "PUBLISH_CLAIM_NOTICE"}', 0),
(123, 1, 'STAGE_CHANGE', '阶段变更', '案件从【债权申报】阶段推进到【债权审查】阶段', 3, '张管理员', 'ADMINISTRATOR', '2026-02-15 18:00:00', '192.168.1.101', '[{"fileId": 5001, "fileName": "债权申报登记表.pdf"}]', '{"transitionId": 2, "fromStage": "CLAIM_REGISTRATION", "toStage": "CLAIM_REVIEW"}', 1),
(123, 1, 'STAGE_CHANGE', '阶段变更', '案件从【债权审查】阶段推进到【财产调查】阶段', 3, '张管理员', 'ADMINISTRATOR', '2026-03-05 17:30:00', '192.168.1.101', '[{"fileId": 5002, "fileName": "债权审查报告.pdf"}]', '{"transitionId": 3, "fromStage": "CLAIM_REVIEW", "toStage": "PROPERTY_INVESTIGATION"}', 1),
(123, 1, 'FILE_UPLOAD', '文件上传', '上传了【财产调查初步报告】', 3, '张管理员', 'ADMINISTRATOR', '2026-03-10 14:00:00', '192.168.1.101', '[{"fileId": 5004, "fileName": "财产调查初步报告.pdf"}]', '{"fileId": 5004, "fileSize": 2097152, "fileCategory": "REPORT"}', 0);
```

### 5.6 用户角色权限示例数据

```sql
-- 假设用户表已存在，插入示例用户角色关系
INSERT INTO `sys_user_role` (`user_id`, `role_id`) VALUES
(1, 1),  -- 系统管理员
(2, 2),  -- 法官
(3, 3),  -- 管理人
(4, 4),  -- 债权人
(5, 5);  -- 书记员

-- 角色表
INSERT INTO `sys_role` (`role_id`, `role_code`, `role_name`, `description`, `status`) VALUES
(1, 'ADMIN', '系统管理员', '系统管理员角色', 'ACTIVE'),
(2, 'JUDGE', '法官', '法院法官角色', 'ACTIVE'),
(3, 'ADMINISTRATOR', '管理人', '破产管理人角色', 'ACTIVE'),
(4, 'CREDITOR', '债权人', '案件债权人角色', 'ACTIVE'),
(5, 'CLERK', '书记员', '法院书记员角色', 'ACTIVE');

-- 用户表
INSERT INTO `sys_user` (`user_id`, `username`, `real_name`, `email`, `phone`, `status`) VALUES
(1, 'admin', '系统管理员', 'admin@example.com', '13800000001', 'ACTIVE'),
(2, 'judge_li', '李法官', 'judge_li@example.com', '13800000002', 'ACTIVE'),
(3, 'admin_zhang', '张管理员', 'admin_zhang@example.com', '13800000003', 'ACTIVE'),
(4, 'creditor_wang', '王债权人', 'creditor_wang@example.com', '13800000004', 'ACTIVE'),
(5, 'clerk_zhao', '赵书记员', 'clerk_zhao@example.com', '13800000005', 'ACTIVE');
```

---

## 6. 实施优先级与技术考量

### 6.1 实施优先级

#### 6.1.1 第一阶段：核心功能（高优先级）

**目标**: 实现基本的工作流管理功能，支持案件阶段的推进和查看。

**功能清单**:
1. 数据库表结构创建
2. 工作流阶段配置管理
3. 案件工作流创建和初始化
4. 获取案件工作流信息API
5. 推进案件到下一阶段API
6. 基础的时间线记录功能
7. 简单的权限控制

**预计工期**: 2-3周

**验收标准**:
- 能够创建案件工作流
- 能够推进案件到下一阶段
- 能够查看案件当前状态和阶段
- 能够查看基本的时间线记录

#### 6.1.2 第二阶段：增强功能（中优先级）

**目标**: 增强工作流管理功能，支持更复杂的业务场景。

**功能清单**:
1. 回退案件到上一阶段
2. 暂停/恢复案件工作流
3. 阶段任务管理
4. 进度更新功能
5. 高级权限控制
6. 数据权限控制
7. 完善的错误处理

**预计工期**: 2周

**验收标准**:
- 能够回退案件到上一阶段
- 能够暂停和恢复案件
- 能够管理阶段任务
- 能够更新阶段进度
- 权限控制完善

#### 6.1.3 第三阶段：优化功能（低优先级）

**目标**: 优化用户体验和系统性能。

**功能清单**:
1. 时间线高级筛选和导出
2. 工作流统计分析
3. 通知功能
4. 性能优化
5. 缓存优化
6. 监控和日志

**预计工期**: 1-2周

**验收标准**:
- 时间线功能完善
- 统计功能可用
- 系统性能达标
- 监控和日志完善

### 6.2 技术考量

#### 6.2.1 性能优化

**数据库层面**:
1. 合理使用索引，避免全表扫描
2. 对大表进行分表分库（按案件ID哈希）
3. 使用读写分离，查询走从库
4. 对历史数据进行归档

**应用层面**:
1. 使用Redis缓存热点数据（工作流信息、阶段配置）
2. 使用异步处理非核心业务（通知、日志）
3. 使用批量操作减少数据库访问
4. 使用连接池优化数据库连接

**查询优化**:
```sql
-- 使用索引优化查询
SELECT * FROM case_workflow_stage 
WHERE case_id = ? AND stage_status = 'IN_PROGRESS';

-- 使用覆盖索引
SELECT instance_id, stage_id, stage_status, progress 
FROM case_workflow_stage 
WHERE case_id = ?;

-- 避免SELECT *
SELECT workflow_id, case_id, current_stage_id, workflow_status, overall_progress 
FROM case_workflow 
WHERE case_id = ?;
```

#### 6.2.2 并发控制

**乐观锁**:
```java
@Version
private Integer version;

// 更新时检查版本号
UPDATE case_workflow 
SET current_stage_id = ?, version = version + 1 
WHERE workflow_id = ? AND version = ?;
```

**悲观锁**:
```java
@Transactional
public void advanceWorkflow(Long caseId) {
    CaseWorkflow workflow = workflowRepository.findByIdWithLock(caseId);
    // 处理逻辑
}
```

**分布式锁**:
```java
// 使用Redis分布式锁
String lockKey = "workflow:advance:" + caseId;
RLock lock = redissonClient.getLock(lockKey);
try {
    if (lock.tryLock(10, 30, TimeUnit.SECONDS)) {
        // 处理逻辑
    }
} finally {
    lock.unlock();
}
```

#### 6.2.3 数据一致性

**事务管理**:
```java
@Transactional(rollbackFor = Exception.class)
public WorkflowTransition advanceWorkflow(Long caseId, Long targetStageId, String reason, Long userId) {
    // 1. 更新当前阶段
    // 2. 更新目标阶段
    // 3. 更新工作流
    // 4. 创建转换记录
    // 5. 创建时间线记录
    // 所有操作在一个事务中，保证一致性
}
```

**最终一致性**:
对于非核心业务（如通知、统计），可以使用消息队列实现最终一致性。

#### 6.2.4 安全性

**SQL注入防护**:
- 使用参数化查询
- 使用ORM框架（JPA、MyBatis）

**XSS防护**:
- 对用户输入进行转义
- 使用前端框架的自动转义功能

**CSRF防护**:
- 使用CSRF Token
- 验证Referer头

**敏感数据保护**:
- 对敏感字段进行加密
- 使用HTTPS传输
- 记录敏感操作日志

#### 6.2.5 可扩展性

**微服务化**:
考虑将工作流服务独立为微服务，便于独立部署和扩展。

**插件化**:
支持自定义阶段和任务，通过配置实现业务扩展。

**API版本管理**:
使用版本号管理API，便于向后兼容。

#### 6.2.6 监控和日志

**日志记录**:
```java
@Slf4j
@Service
public class WorkflowService {
    
    public WorkflowTransition advanceWorkflow(Long caseId, Long targetStageId, String reason, Long userId) {
        log.info("开始推进案件工作流, caseId={}, targetStageId={}, userId={}", caseId, targetStageId, userId);
        
        try {
            // 处理逻辑
            
            log.info("案件工作流推进成功, caseId={}, transitionId={}", caseId, transition.getTransitionId());
            return transition;
        } catch (Exception e) {
            log.error("案件工作流推进失败, caseId={}, error={}", caseId, e.getMessage(), e);
            throw e;
        }
    }
}
```

**监控指标**:
- 工作流推进成功率
- 平均处理时间
- 并发请求数
- 错误率

**告警机制**:
- 工作流推进失败告警
- 性能指标异常告警
- 系统资源告警

### 6.3 前端集成点

#### 6.3.1 API集成

**基础URL**: `http://localhost:8080/api/v1`

**认证方式**: Bearer Token

**请求头**:
```
Authorization: Bearer {token}
Content-Type: application/json
```

#### 6.3.2 前端数据模型

**工作流信息**:
```typescript
interface CaseWorkflow {
  caseId: number;
  caseNo: string;
  caseName: string;
  currentStage: WorkflowStage;
  stages: WorkflowStage[];
  overallProgress: number;
  estimatedCompletionDate: string;
  workflowStatus: string;
}

interface WorkflowStage {
  stageId: number;
  stageCode: string;
  stageName: string;
  stageOrder: number;
  status: string;
  startTime?: string;
  endTime?: string;
  duration?: number;
  progress: number;
  isOptional: boolean;
}
```

**时间线记录**:
```typescript
interface TimelineRecord {
  timelineId: number;
  caseId: number;
  operationType: string;
  operationName: string;
  operationContent: string;
  operatorId: number;
  operatorName: string;
  operatorRole: string;
  operationTime: string;
  attachments: Attachment[];
  metadata: Record<string, any>;
  isKeyEvent: boolean;
}
```

#### 6.3.3 前端调用示例

**获取工作流信息**:
```javascript
async function getWorkflow(caseId) {
  const response = await fetch(`/api/v1/case-workflow/${caseId}`, {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
  const data = await response.json();
  return data.data;
}
```

**推进工作流**:
```javascript
async function advanceWorkflow(caseId, targetStageId, reason) {
  const response = await fetch(`/api/v1/case-workflow/${caseId}/advance`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      targetStageId,
      reason
    })
  });
  const data = await response.json();
  return data.data;
}
```

**获取时间线**:
```javascript
async function getTimeline(caseId, params) {
  const queryParams = new URLSearchParams(params).toString();
  const response = await fetch(`/api/v1/case-workflow/${caseId}/timeline?${queryParams}`, {
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
  const data = await response.json();
  return data.data;
}
```

### 6.4 测试策略

#### 6.4.1 单元测试

**测试覆盖**:
- Service层业务逻辑
- Repository层数据访问
- 工具类方法

**测试框架**: JUnit 5 + Mockito

#### 6.4.2 集成测试

**测试场景**:
- 完整的工作流推进流程
- 阶段转换规则验证
- 权限控制验证
- 异常处理验证

**测试框架**: Spring Boot Test + TestContainers

#### 6.4.3 性能测试

**测试工具**: JMeter

**测试指标**:
- 响应时间
- 吞吐量
- 并发用户数
- 资源使用率

#### 6.4.4 压力测试

**测试场景**:
- 高并发工作流推进
- 大量数据查询
- 长时间运行稳定性

### 6.5 部署策略

#### 6.5.1 环境配置

**开发环境**:
- 单机部署
- 使用H2内存数据库
- 日志级别DEBUG

**测试环境**:
- 单机部署
- 使用MySQL数据库
- 日志级别INFO

**生产环境**:
- 集群部署（至少2个节点）
- 使用MySQL主从
- 日志级别WARN
- 启用监控和告警

#### 6.5.2 部署步骤

1. 准备数据库和执行脚本
2. 配置应用参数
3. 打包应用
4. 部署到服务器
5. 启动应用
6. 验证部署结果
7. 配置监控和告警

#### 6.5.3 回滚策略

1. 保留旧版本应用
2. 数据库变更使用版本控制
3. 使用蓝绿部署或金丝雀发布
4. 准备回滚脚本

---

## 附录

### A. 错误码完整列表

| 错误码 | 错误类型 | 说明 | 处理建议 |
|--------|---------|------|----------|
| 200 | 成功 | 请求成功 | - |
| 400 | 参数错误 | 请求参数不正确 | 检查请求参数 |
| 401 | 未授权 | Token无效或过期 | 重新登录 |
| 403 | 禁止访问 | 权限不足 | 联系管理员 |
| 404 | 资源不存在 | 请求的资源不存在 | 检查资源ID |
| 409 | 状态冲突 | 当前状态不允许该操作 | 检查当前状态 |
| 422 | 业务规则错误 | 违反业务规则 | 查看错误详情 |
| 500 | 服务器错误 | 服务器内部错误 | 联系技术支持 |
| WF_001 | 业务规则错误 | 当前阶段未完成，无法推进 | 完成当前阶段 |
| WF_002 | 业务规则错误 | 目标阶段的前置阶段未完成 | 完成前置阶段 |
| WF_003 | 业务规则错误 | 案件已暂停，无法推进 | 恢复案件 |
| WF_004 | 业务规则错误 | 案件已完成，无法推进 | 案件已结束 |
| WF_005 | 业务规则错误 | 无权限执行此操作 | 联系管理员 |
| WF_006 | 业务规则错误 | 必填任务未完成 | 完成必填任务 |
| WF_007 | 业务规则错误 | 阶段依赖关系不满足 | 检查依赖关系 |
| WF_008 | 业务规则错误 | 回退阶段无效 | 检查回退阶段 |

### B. 数据库变更脚本

**版本**: v1.0.0

**变更日期**: 2026-01-19

**变更内容**: 创建案件工作流相关表

```sql
-- 执行顺序：
-- 1. 创建核心表
-- 2. 创建索引
-- 3. 创建外键
-- 4. 创建触发器
-- 5. 插入初始数据

-- 回滚脚本：
-- DROP TRIGGER IF EXISTS `trg_create_timeline_on_transition`;
-- DROP TRIGGER IF EXISTS `trg_update_workflow_progress`;
-- DROP TABLE IF EXISTS `case_stage_task`;
-- DROP TABLE IF EXISTS `stage_task`;
-- DROP TABLE IF EXISTS `workflow_timeline`;
-- DROP TABLE IF EXISTS `workflow_transition`;
-- DROP TABLE IF EXISTS `case_workflow_stage`;
-- DROP TABLE IF EXISTS `workflow_stage_config`;
-- DROP TABLE IF EXISTS `case_workflow`;
```

### C. API文档

**Swagger地址**: `http://localhost:8080/swagger-ui.html`

**API文档格式**: OpenAPI 3.0

**主要端点**:
- `/api/v1/case-workflow/{caseId}` - 获取工作流信息
- `/api/v1/case-workflow/{caseId}/advance` - 推进工作流
- `/api/v1/case-workflow/{caseId}/rollback` - 回退工作流
- `/api/v1/case-workflow/{caseId}/suspend` - 暂停工作流
- `/api/v1/case-workflow/{caseId}/timeline` - 获取时间线
- `/api/v1/workflow-stages` - 获取阶段配置

---

**文档结束**
