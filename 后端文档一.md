# 后端开发模块一:用户权限与认证管理模块

## 一、模块名称与核心功能描述

### 模块名称
用户权限与认证管理模块 (User Authentication & Authorization Module)

### 核心功能
1. 用户管理:用户注册、信息维护、状态管理、密码管理
2. 角色权限管理:角色定义、权限分配、角色权限关联
3. 登录认证:用户登录、短信验证码、登录失败记录、登录日志
4. Token管理:Token生成、验证、撤销、过期管理
5. 系统配置:系统参数配置管理
6. 操作日志:用户操作记录、系统活动追踪

## 二、涉及的数据库表及操作

### 主要数据库表

#### 1. 用户相关表
- **tb_user** - 用户表
  - 操作:增删改查、状态更新、密码修改、登录信息更新
  - 索引:username, mobile, email, status, create_time

- **tb_role** - 角色表
  - 操作:增删改查、状态管理、排序
  - 索引:role_code, status

- **tb_permission** - 权限表
  - 操作:增删改查、树形结构维护、状态管理
  - 索引:perm_code, parent_id, perm_type, status

- **tb_user_role** - 用户角色关联表
  - 操作:用户角色分配、批量分配、删除
  - 索引:user_id, role_id

- **tb_role_permission** - 角色权限关联表
  - 操作:角色权限分配、批量分配、删除
  - 索引:role_id, perm_id

#### 2. 认证相关表
- **tb_token** - Token表
  - 操作:Token创建、验证、撤销、清理过期Token
  - 索引:user_id, token_value, expire_time, status

- **tb_sms_code** - 短信验证码表
  - 操作:验证码生成、验证、使用状态更新
  - 索引:mobile, expire_time, used_status

- **tb_login_fail** - 登录失败记录表
  - 操作:失败记录、失败次数统计、解锁时间管理
  - 索引:login_account, fail_ip, last_fail_time

- **tb_login_record** - 登录记录表
  - 操作:登录日志记录、登录历史查询
  - 索引:user_id, user_account, login_time, login_status, create_time

#### 3. 系统相关表
- **tb_system_config** - 系统配置表
  - 操作:配置增删改查、分组查询
  - 索引:config_key, config_group, status

- **tb_activity** - 系统操作日志表
  - 操作:操作日志记录、日志查询、日志统计
  - 索引:user_id, type, status, create_time

- **tb_notification** - 系统通知表
  - 操作:通知创建、已读状态更新、通知查询
  - 索引:user_id, is_read, type, status, create_time

- **tb_todo** - 系统待办事项表
  - 操作:待办创建、状态更新、完成、删除
  - 索引:user_id, status, deadline, create_time

## 三、主要API接口定义

### 3.1 用户管理接口

#### 用户注册
```
POST /api/v1/user/register
Request:
{
  "username": "string",
  "password": "string",
  "realName": "string",
  "mobile": "string",
  "email": "string",
  "smsCode": "string"
}
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": "long",
    "username": "string",
    "token": "string"
  }
}
```

#### 用户登录
```
POST /api/v1/user/login
Request:
{
  "username": "string",
  "password": "string",
  "smsCode": "string"  // 可选,短信验证码登录
}
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": "long",
    "username": "string",
    "realName": "string",
    "token": "string",
    "permissions": ["string"]
  }
}
```

#### 获取用户信息
```
GET /api/v1/user/info
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "long",
    "username": "string",
    "realName": "string",
    "mobile": "string",
    "email": "string",
    "status": "string",
    "roles": ["string"],
    "permissions": ["string"]
  }
}
```

#### 更新用户信息
```
PUT /api/v1/user/{userId}
Request:
{
  "realName": "string",
  "mobile": "string",
  "email": "string",
  "phone": "string"
}
Response:
{
  "code": 200,
  "message": "success"
}
```

#### 修改密码
```
PUT /api/v1/user/password
Request:
{
  "oldPassword": "string",
  "newPassword": "string"
}
Response:
{
  "code": 200,
  "message": "success"
}
```

#### 用户列表(分页)
```
GET /api/v1/user/list?pageNum=1&pageSize=10&status=ACTIVE&keyword=xxx
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "list": [...]
  }
}
```

#### 用户状态管理
```
PUT /api/v1/user/{userId}/status
Request:
{
  "status": "ACTIVE|INACTIVE|LOCKED|DELETED"
}
Response:
{
  "code": 200,
  "message": "success"
}
```

### 3.2 角色权限管理接口

#### 角色列表
```
GET /api/v1/role/list
Response:
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "long",
      "roleCode": "string",
      "roleName": "string",
      "roleDesc": "string",
      "status": "string"
    }
  ]
}
```

#### 创建角色
```
POST /api/v1/role
Request:
{
  "roleCode": "string",
  "roleName": "string",
  "roleDesc": "string",
  "status": "ACTIVE"
}
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "id": "long"
  }
}
```

#### 分配角色权限
```
POST /api/v1/role/{roleId}/permissions
Request:
{
  "permissionIds": [1, 2, 3, 4, 5]
}
Response:
{
  "code": 200,
  "message": "success"
}
```

#### 获取角色权限
```
GET /api/v1/role/{roleId}/permissions
Response:
{
  "code": 200,
  "message": "success",
  "data": [1, 2, 3, 4, 5]
}
```

#### 分配用户角色
```
POST /api/v1/user/{userId}/roles
Request:
{
  "roleIds": [1, 2, 3]
}
Response:
{
  "code": 200,
  "message": "success"
}
```

#### 权限树查询
```
GET /api/v1/permission/tree
Response:
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": "long",
      "permCode": "string",
      "permName": "string",
      "permType": "string",
      "parentId": "long",
      "children": []
    }
  ]
}
```

### 3.3 认证相关接口

#### 发送短信验证码
```
POST /api/v1/auth/sms-code
Request:
{
  "mobile": "string",
  "smsType": "1"  // 1-登录, 2-注册, 3-找回密码
}
Response:
{
  "code": 200,
  "message": "success"
}
```

#### 验证Token
```
POST /api/v1/auth/verify-token
Request:
{
  "token": "string"
}
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "userId": "long",
    "username": "string",
    "valid": true
  }
}
```

#### 登出
```
POST /api/v1/auth/logout
Response:
{
  "code": 200,
  "message": "success"
}
```

#### 登录历史查询
```
GET /api/v1/auth/login-history?pageNum=1&pageSize=10
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "list": [...]
  }
}
```

### 3.4 系统配置接口

#### 获取系统配置
```
GET /api/v1/config?configGroup=xxx
Response:
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "configKey": "string",
      "configValue": "string",
      "configDesc": "string",
      "configGroup": "string"
    }
  ]
}
```

#### 更新系统配置
```
PUT /api/v1/config
Request:
{
  "configKey": "string",
  "configValue": "string"
}
Response:
{
  "code": 200,
  "message": "success"
}
```

### 3.5 通知与待办接口

#### 获取通知列表
```
GET /api/v1/notification/list?pageNum=1&pageSize=10&isRead=0
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "list": [...]
  }
}
```

#### 标记通知已读
```
PUT /api/v1/notification/{notificationId}/read
Response:
{
  "code": 200,
  "message": "success"
}
```

#### 获取待办列表
```
GET /api/v1/todo/list?pageNum=1&pageSize=10&status=PENDING
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "list": [...]
  }
}
```

#### 完成待办
```
PUT /api/v1/todo/{todoId}/complete
Response:
{
  "code": 200,
  "message": "success"
}
```

### 3.6 操作日志接口

#### 操作日志查询
```
GET /api/v1/activity/list?pageNum=1&pageSize=10&userId=xxx&type=xxx
Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "list": [...]
  }
}
```

## 四、与其他模块的边界划分

### 4.1 依赖关系
- **本模块作为基础模块**,为其他模块提供:
  - 用户身份认证服务(Token验证)
  - 权限验证服务(基于角色权限)
  - 用户信息查询服务
  - 操作日志记录服务
  - 通知推送服务

### 4.2 接口边界
- **本模块不直接操作**:
  - 案件相关表(tb_bankrupt_case, tb_court等)
  - 债权相关表(tb_creditor_claim, tb_creditor_info等)
  - 资金相关表(tb_fund_account, tb_fund_flow等)
  - 工作团队相关表(tb_work_team, tb_work_team_member等)

- **本模块提供的服务**:
  - 用户身份验证:其他模块通过Token验证用户身份
  - 权限验证:其他模块调用权限验证接口检查用户权限
  - 用户信息:其他模块通过用户ID查询用户基本信息
  - 日志记录:其他模块调用日志记录接口记录操作

### 4.3 数据交互
- **其他模块调用本模块接口**:
  - 验证用户Token有效性
  - 检查用户权限
  - 获取用户信息
  - 记录操作日志
  - 发送系统通知

- **本模块不主动调用其他模块**:
  - 保持独立性,不依赖业务模块

### 4.4 WebSocket通知
- 本模块负责WebSocket连接管理和消息推送
- 其他模块通过接口调用发送通知
- 通知类型:系统通知、审核通知、状态变更通知等

## 五、开发优先级与时间预估

### 5.1 开发优先级

#### P0 - 最高优先级(必须优先完成)
1. 用户注册与登录功能
   - 用户注册接口
   - 用户登录接口
   - 短信验证码发送与验证
   - Token生成与验证
   - 登录失败记录与锁定机制

2. 基础权限管理
   - 角色管理接口
   - 权限树查询接口
   - 用户角色分配接口
   - 角色权限分配接口
   - 权限验证中间件

3. 用户信息管理
   - 获取用户信息接口
   - 更新用户信息接口
   - 修改密码接口
   - 用户状态管理接口

#### P1 - 高优先级
1. 系统配置管理
   - 系统配置查询接口
   - 系统配置更新接口

2. 登录日志管理
   - 登录记录接口
   - 登录历史查询接口

3. 操作日志管理
   - 操作日志记录接口
   - 操作日志查询接口

#### P2 - 中优先级
1. 通知管理
   - 通知创建接口
   - 通知列表查询接口
   - 标记已读接口
   - WebSocket通知推送

2. 待办管理
   - 待办创建接口
   - 待办列表查询接口
   - 完成待办接口

#### P3 - 低优先级
1. 高级功能
   - 用户批量导入
   - 角色权限批量操作
   - 日志统计分析
   - 系统配置分组管理

### 5.2 时间预估

#### 总体时间: 12-15个工作日

#### 详细分解:

**P0阶段(5-6个工作日)**
- 用户注册与登录: 2天
  - 用户注册接口: 0.5天
  - 用户登录接口: 0.5天
  - 短信验证码: 0.5天
  - Token管理: 0.5天

- 基础权限管理: 2天
  - 角色管理接口: 0.5天
  - 权限树查询: 0.5天
  - 用户角色分配: 0.5天
  - 权限验证中间件: 0.5天

- 用户信息管理: 1-2天
  - 用户信息接口: 0.5天
  - 密码修改: 0.5天
  - 状态管理: 0.5天
  - 单元测试: 0.5天

**P1阶段(2-3个工作日)**
- 系统配置管理: 1天
- 登录日志管理: 1天
- 操作日志管理: 1天

**P2阶段(2-3个工作日)**
- 通知管理: 1.5天
  - 基础接口: 1天
  - WebSocket推送: 0.5天
- 待办管理: 1-1.5天

**P3阶段(2-3个工作日)**
- 高级功能: 2-3天
- 集成测试: 1天

### 5.3 里程碑

| 里程碑 | 完成时间 | 交付内容 |
|--------|---------|---------|
| M1 | 第6天 | P0功能完成,用户可以注册登录,基础权限可用 |
| M2 | 第9天 | P1功能完成,系统配置和日志管理可用 |
| M3 | 第12天 | P2功能完成,通知和待办功能可用 |
| M4 | 第15天 | 全部功能完成,通过集成测试 |

### 5.4 并行开发建议

- 本模块作为基础模块,建议**优先开发**
- 其他模块可以并行开发,但需要:
  - 使用Mock数据模拟本模块接口
  - 待本模块P0功能完成后,进行联调
  - 本模块提供接口文档和Mock服务

## 六、技术要点

### 6.1 安全要求
- 密码使用BCrypt加密存储
- Token使用JWT,有效期7天
- 短信验证码有效期5分钟
- 登录失败5次锁定账号30分钟
- 敏感操作需要二次验证

### 6.2 性能要求
- 用户信息查询响应时间 < 100ms
- 权限验证响应时间 < 50ms
- Token验证响应时间 < 30ms
- 支持并发用户数 > 1000

### 6.3 缓存策略
- 用户权限信息缓存到Redis,有效期1小时
- 系统配置缓存到Redis,有效期24小时
- Token黑名单缓存到Redis,有效期与Token一致

### 6.4 监控告警
- 登录失败率监控
- 异常登录IP监控
- Token异常使用监控
- 系统配置变更审计

## 七、测试要求

### 7.1 单元测试
- 核心业务逻辑单元测试覆盖率 > 80%
- 权限验证逻辑单元测试覆盖率 > 90%

### 7.2 接口测试
- 所有接口进行接口测试
- 异常场景测试覆盖

### 7.3 性能测试
- 登录接口:支持100 TPS
- 权限验证:支持500 TPS
- 并发用户测试:1000并发用户

### 7.4 安全测试
- SQL注入测试
- XSS攻击测试
- CSRF攻击测试
- 暴力破解测试
