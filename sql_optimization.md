# SQL查询优化方案

## 问题分析

通过分析SQL执行计划，发现以下主要性能瓶颈：

1. **缺失索引**：执行计划显示STKPRHS1表存在两个高影响度的缺失索引，影响分别为18.7827和28.1141
2. **全表扫描**：多个表执行全表或聚集索引扫描，特别是STKPRHS1表（成本0.275782）
3. **嵌套子查询**：SQL中包含多个TOP 1子查询，导致性能下降
4. **排序操作**：存在成本较高的Sort操作（0.369135）
5. **嵌套循环连接**：大量使用嵌套循环连接，处理大量数据时效率低下

## 优化方案

### 1. 添加缺失索引

根据执行计划建议，为STKPRHS1表添加高效索引：

```sql
-- 优化缺失索引1：TYPE列作为等值过滤条件
CREATE NONCLUSTERED INDEX IX_STKPRHS1_TYPE_INCLUDE
ON STKPRHS1 ([TYPE])
INCLUDE ([ERI], [SUPP], [SDATE], [CODE], [PROJNO], [GWN], [TOTAL], [QTY], [EDITOR], [CURR], [INVNO], [STAFF], [SCRUTINY], [ISMOLDSPHS]);

-- 优化缺失索引2：TYPE和GWN列作为等值过滤条件
CREATE NONCLUSTERED INDEX IX_STKPRHS1_TYPE_GWN_INCLUDE
ON STKPRHS1 ([TYPE], [GWN])
INCLUDE ([ERI], [SUPP], [SDATE], [CODE], [PROJNO], [TOTAL], [QTY], [EDITOR], [CURR], [INVNO], [STAFF], [SCRUTINY], [ISMOLDSPHS]);
```

### 2. 优化子查询

将SQL中的子查询转换为JOIN操作，减少嵌套查询的开销：

```sql
-- 优化后的SQL语句
SELECT 
    STKPRHS1.ERI,
    STKPRHS1.CODE,
    A.ABBR AS SUPPABBR,
    ORDCORD2.CODE AS 订单号,
    STKPRHS2.PROD AS 物料代码,
    STKPRHS1.SDATE,
    PRODUCT.CNAME AS 物料名称,
    STKPRHS2.SPEC AS 物料规格,
    STKPRHS2.VPNO AS 批号,
    STKPRHS2.REMARK AS 备注,
    PRODUNIT1.CNAME AS 单位,
    STKPRHS2.QTY AS 收货数量,
    PRODUNIT2.CNAME AS 辅助单位,
    STKPRHS2.OUQTY AS 辅助数量,
    PRODQTY.CQTY AS 库存,
    A.CLASNAME AS SUPPCLASNAME,
    STKPRHS1.SUPP,
    STKPRHS1.STAFF,
    S.CNAME AS 采购员姓名,
    STKPRHS1.QTY,
    STKPRHS1.CURR,
    STKPRHS1.TOTAL,
    STKPRHS1.INVNO,
    STKPRHS1.PROJNO,
    STKPRHS1.GWN,
    B.NAME AS 仓库名称,
    STKPRHS1.EDITOR,
    STKPRHS1.SCRUTINY 
FROM STKPRHS1 
LEFT JOIN (
    SELECT SUPPLIER.CODE AS SUPPCODE, SUPPLIER.ABBR, SUPPCLAS.NAME AS CLASNAME 
    FROM SUPPLIER 
    LEFT JOIN SUPPCLAS ON SUPPCLAS.CODE = SUPPLIER.CLAS
) A ON A.SUPPCODE = STKPRHS1.SUPP 
LEFT JOIN STAFFER S ON S.CODE = STKPRHS1.STAFF 
LEFT JOIN (SELECT CODE, NAME FROM GODOWN) B ON B.CODE = STKPRHS1.GWN 
LEFT JOIN STKPRHS2 ON STKPRHS2.MASTERI = STKPRHS1.ERI
-- 优化：使用JOIN替代子查询
LEFT JOIN PRODQTY ON PRODQTY.GWN = STKPRHS2.GWN AND PRODQTY.PROD = STKPRHS2.PROD
LEFT JOIN PRODUNIT PRODUNIT1 ON PRODUNIT1.CODE = STKPRHS2.UNIT
LEFT JOIN ORDCORD2 ON ORDCORD2.ERI = STKPRHS2.ORDERI
LEFT JOIN PRODUNIT PRODUNIT2 ON PRODUNIT2.CODE = STKPRHS2.OTHERUNIT
LEFT JOIN PRODUCT ON PRODUCT.CODE = STKPRHS2.PROD
LEFT JOIN ORDSPHS2 ON ORDSPHS2.ERI = STKPRHS2.SRCERI
LEFT JOIN (
    SELECT ORDBOM1.SRCERI, ORDBOM2.QTY, ORDBOM2.SUBPROD 
    FROM ORDBOM1 
    LEFT JOIN ORDBOM2 ON ORDBOM2.MASTERI = ORDBOM1.ERI
) K ON K.SRCERI = STKPRHS2.ORDERI AND K.SUBPROD = STKPRHS2.PROD 
WHERE STKPRHS1.TYPE = 0 
AND ISNULL(STKPRHS1.ISMOLDSPHS, 0) = 0 
AND STKPRHS1.GWN IN (
    SELECT CODE FROM GODOWN 
    WHERE (CODE IN (SELECT GWN FROM GWNSTAFF WHERE STAFF = 'admin')  OR GWNSTAFFON = 0)
)  
ORDER BY STKPRHS1.SDATE DESC, STKPRHS1.CODE DESC;
```

### 3. 为其他表添加索引

为频繁用于连接和筛选的列添加索引：

```sql
-- 为PRODQTY表添加索引，优化GWN和PROD的JOIN
CREATE NONCLUSTERED INDEX IX_PRODQTY_GWN_PROD
ON PRODQTY ([GWN], [PROD])
INCLUDE ([CQTY]);

-- 为ORDCORD2表添加索引，优化ERI列的JOIN
CREATE NONCLUSTERED INDEX IX_ORDCORD2_ERI
ON ORDCORD2 ([ERI])
INCLUDE ([CODE]);

-- 为STKPRHS2表添加索引，优化MASTERI和ORDERI列
CREATE NONCLUSTERED INDEX IX_STKPRHS2_MASTERI_ORDERI
ON STKPRHS2 ([MASTERI], [ORDERI])
INCLUDE ([PROD], [SPEC], [QTY], [UNIT], [OTHERUNIT], [OUQTY], [VPNO], [REMARK], [GWN], [SRCERI]);

-- 为GWNSTAFF表添加索引，优化STAFF列的筛选
CREATE NONCLUSTERED INDEX IX_GWNSTAFF_STAFF
ON GWNSTAFF ([STAFF]);
```

### 4. 优化WHERE条件

- 将`ISNULL(STKPRHS1.ISMOLDSPHS, 0) = 0`优化为`(STKPRHS1.ISMOLDSPHS = 0 OR STKPRHS1.ISMOLDSPHS IS NULL)`，避免函数对索引的影响
- 考虑将GWN的IN子查询优化为EXISTS或JOIN

### 5. 优化排序

- 考虑在STKPRHS1表上添加包含SDATE和CODE的索引，以避免排序操作

```sql
-- 优化排序的索引
CREATE NONCLUSTERED INDEX IX_STKPRHS1_SDATE_CODE
ON STKPRHS1 ([SDATE] DESC, [CODE] DESC)
INCLUDE ([TYPE], [ISMOLDSPHS], [GWN]);
```

## 预期效果

1. **索引优化**：减少全表扫描，提高查询速度
2. **子查询优化**：减少嵌套查询开销，提高查询效率
3. **排序优化**：避免不必要的排序操作
4. **连接优化**：提高连接操作的效率

通过以上优化，预计查询性能将显著提升，卡顿问题得到解决。